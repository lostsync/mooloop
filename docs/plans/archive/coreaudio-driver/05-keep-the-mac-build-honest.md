# 05 Keep the Mac build honest

A platform nothing builds is a platform that is already broken. Add a macOS job
to `.github/workflows/ci.yml`, and bring `README.md`, `CURRENT.md`,
`ARCHITECTURE.md`, `AUDIO_ARCHITECTURE.md` and `OPERATIONS.md` into line with
two drivers. Then this directory moves to `archive/`.
