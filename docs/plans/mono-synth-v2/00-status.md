Not started. Work 01 → 08 in order.

01 is the contract every other step refers to and should be read first even
when picking up a later step. 02 is shared work that also lands on Poly —
`docs/plans/poly-synth-v2/00-status.md` names it as a prerequisite, so do not
duplicate it there.

03 (held-note stack) is independent of 04-06 (filter voicing) and can be done
either way round, but the spec's order puts note behaviour first so that the
filter work is voiced against an instrument that already plays like a
monosynth. 08 is the listening pass and cannot start until 02-07 are in.

Source spec: `~/Downloads/mooloop_synth_v2_spec.md`, sections 4, 5, 8-13.
