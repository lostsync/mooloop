# Mooloop — dev journal

*The early entries were reconstructed from the commit log; dates there are derived from relative timestamps, so treat them as approximate. A few commits are out of chronological order in the log itself (rebase/cherry-pick noise) and are placed by content rather than by position. From the modulation-grid entry onward it is kept live — a dated section per arc of work, not per commit.*

---

## Aug 11 — Phase 0: it builds, it registers, it makes no sound

Scaffolded the workspace as five crates: `mooloop-core` (musical time types, lock-free bridge enums), `mooloop-dsp` (a `Device` trait and a throwaway metronome), `mooloop-engine` (JACK via pipewire-jack, realtime transport, rtrb SPSC bridge), `mooloop-ui` (Slint, dark toolbar, placeholder rack), and `mooloop-app` to wire them together (`b8a3fd1`). Clean build, clean clippy, two passing tests, a live JACK client.

And silence. PipeWire's JACK bridge doesn't auto-wire a new client's outputs to the playback sink, so the ports existed with no links attached. Connected `mooloop:out_{l,r}` to `system:playback_{1,2}` after activation, best-effort with a hint toward the patchbay if it fails (`9ba4dbe`). First of two silence bugs that looked identical from the outside and had nothing in common underneath.

## Aug 12 — Phase 1: a real instrument

Replaced the metronome with a sampler and a 16-step sequencer, FL-style (`5f95c88`). Pattern/Step data model with velocity carried from the start for forward compatibility. ADSR, sample-accurate triggers, start/loop points, three loop modes, linear-interpolated resampling, and a synthesized default kick so it's audible out of the box. Samples publish through an ArcSwap slot so nothing allocates on the audio thread. WAV loading via zenity plus hound. Half-open `[start, end)` step-boundary intervals so blocks don't double-fire or drop a step.

## Aug 17 — Diagnostics, then the bug they found

A "no playback, no VU" report I couldn't localize by staring at it. Rather than guess a layer, built tools that test each one independently (`14f4991`): a headless `engine-selftest` binary driving the full JACK path, `MOOLOOP_AUTODRIVE=1` driving the real Slint callbacks and reporting whether commands actually forward, an offline sequencer→sampler render test, and env-gated debug logging. All of it passed — which is what made the real cause findable.

It was hound. `samples::<f32>()` only reads IEEE-float WAVs; on ordinary 16-bit PCM every sample yields an error, and the decoder was doing `filter_map(Result::ok)`. Every error silently dropped, empty buffer, sampler treats zero-length as instant silence, no message anywhere (`a51b78d`). Decode at native width now, propagate errors loudly, regression tests both ways.

Same day, Phase 2: 16 channels, an 8-pattern bank, pattern-indexed step edits so editing a non-playing pattern persists, per-channel mute (`1881f30`). Then the engine hardening pass that everything since has stood on (`7e58b4b`): preallocated `StereoBus`, a fixed-capacity sample-timed `EventList` in the shape VST3/CLAP/LV2 use, an `AudioNode` trait replacing `Device`, segment-based sampler rendering split at event offsets, epsilon-tolerant boundary detection (`BOUNDARY_EPS = 1e-6` ticks) proven by a test asserting exactly 400 note-ons across 100 bars of irregular block sizes, `frames_played` as integer ground truth, per-channel channel strips summing into a master, xrun counting, and sample loading moved off the UI thread because the OS was marking the app frozen while the file dialog was up.

## Aug 18 — The long day

Notes became tick-addressed (`5a2dc1d`) and the piano roll became expressive (`d6702ce`), with the velocity lane pinned to and aligned with the grid. Wrote down the buffer-centric product direction and the always-running channel buffer model (`f7bb6c2`, `6a7ae8c`) — the thing the whole instrument is eventually for. Channel volume/pan, a layered song playlist, and playback decoupled from whichever view is open (`21ec5de`, `a6f8bf4`, `acb0790`).

The UI stopped being ad hoc. Rack, sampler editor and transport had each grown their own controls, so a knob in one panel didn't read like a knob in another. Established a shared vocabulary (`dbc7d64`): a `KnobFace` worn by both the full labelled knob and the compact rack knob so they can't drift, LED-segment metering coloured by scale position the way hardware does, latching clip indicators, an `EnvelopeEditor` with fixed stage-width budgets so dragging one handle doesn't reflow the others. Adopted it across the app (`ee1650e`), which handed 108px back to the step grid.

Two input bugs, both Slint routing:

- Every control needed two clicks. `FocusScope` consumes the press that focuses it and ignores presses once focused, and the focus scopes were declared after the touch areas — so the first press was eaten and the second fell through (`cc40eab`). Fix: `focus-on-click: false`, take focus explicitly from the touch area. Regression test dispatches real pointer events rather than invoking callbacks, deliberately avoiding accessibility default actions since those bypass the routing at fault.
- Per-cell `TouchArea`s can't support a drag at all: the cell where the press lands keeps the grab. The rack grid moved to one hit area deriving the cell from mouse-x (`aead814`). Paint and stretch report a *target* state rather than toggling, so dragging across mixed steps doesn't invert each one.

Also in `aead814`: the toolbar split into a transport row (state of playback) and an edit row (what the next click does), patterns became a stepper plus a jump menu that costs the same width at two patterns or two hundred, and cells gained an onset mask beside the coverage mask — before that, a ×2 ratchet, a ×4 ratchet and one sustained note all drew as a full cell.

Project bundles landed as a versioned, durable contract with offline export sharing render state (`57efa44` → `5adb7dd`). DrumSynth and MonoSynth DSP nodes were written against the existing `AudioNode` trait but deliberately not wired to anything yet (`ae3e742`). And documented the headless agent output plus the two gotchas that make it silently screenshot the wallpaper (`3b0ca2f`, `c4d45a5`) — software rendering is the right default for looking at the UI, not the live GPU path.

## Aug 19 — Synths become instruments

Generated sources exposed as channel instruments (`32b50e3`), drums made punchier (`77e2320`).

The mono synth clicked constantly and the reason was structural: `Adsr::note_on` reset level to zero, so every note landing while the voice still sounded stepped the output through zero — which, at the default 150ms release, is most notes in a pattern. Attack now starts from wherever the envelope already is, velocity slides in over 5ms, and block-rate parameters that scale the signal directly get a one-pole `Smoothed` (`1ab8f85`). Added the LFO as one shape with a depth per destination rather than a destination selector, bipolar shapes leaving zero rising so a retrigger isn't itself a click.

Piano roll notes became genuinely draggable (`72bf20a`) — the same defect as the rack grid, one layer up: each note's touch areas lived inside a rectangle bound to that note's tick, so the moment a drag updated the model the rectangle moved out from under the cursor and the delta collapsed to zero. Slint's binding-loop checker rejects the self-recursive scan needed to resolve a note from a press position, so the lookup moved to Rust behind a pure callback, scanning back to front so overlaps resolve to the note drawn on top.

Presets for generators and whole channels in a well-known config directory (`9d44591`), global swing (`dcff827`), and a real menu bar built from pieces that know nothing about which actions exist, so adding an item costs one line (`77334ea`).

## Aug 20 — A composition language

Stepped back from features and defined the instrument UI composition language and its layout/selector primitives (`a6e0d52`, `ee32937`), then rebuilt the source editors as instrument panels (`1399e50`) and, after sketching alternatives (`8d83f8f`), as horizontal rack devices (`6855ac2`).

## Aug 21 — Public release, then the effect suite

Prepared the repo for public release: full GPL terms, an honest README about what this experimental prototype is and isn't, songs saved as openable document files (`dc59381` → `06cdb90`).

Two long-standing annoyances got real diagnoses. Dev builds were OOMing on the 16GB workstation, mostly linking mooloop-ui's Slint-heavy test binaries — capped jobs, switched to mold, trimmed debuginfo (`979f276`). And drum decays sounded ~9.2× longer than their labels promised: `ExpDecay::set_time` treated its argument as the raw 1/e time constant, but level only becomes inaudible after ~9.21 of those, which is why a 39ms setting produced a 363ms kick (`e0d6291`).

Then the effects, done in the right order. Design first (`b81fa96`, `a339763`): descriptors as the single source of truth for range and normalization, engine-owned base value plus modulation offset so knobs and modulators stop fighting, and an explicit record of what's deferred so a later pass doesn't mistake absence for oversight. Filter as a vertical slice — core types, DSP node, engine path, UI face (`a9cf6c9` → `2a4f146`). The realtime path is the interesting part: `Box<dyn AudioNode>` isn't Copy and core mustn't depend on dsp, so structural changes ride a second rtrb pair, with displaced boxes returning via `StructuralReclaim` so the audio thread never frees memory.

With one effect proving the plumbing, generalized before adding more (`97139ac`): tagged `EffectParams`, static `ParamDescriptor` tables, one positional param-changed callback instead of one per knob. Drive (four curves, 2× oversampled so harmonics don't fold back as fizz) and bitcrush (deliberately not oversampled — its aliasing *is* the effect) followed cheaply, which was the point.

Also pinned the exact-version Slint 1.17.1 docs into `AGENTS.md` (`3a064d1`), started `CONTRIBUTORS.md` as a sign-in sheet for which model/harness combinations worked on what (`c146e47`), and made verification proportional to change risk (`36f75fd`).

## Aug 22 — Delay, dynamics, and the mixer

The delay's buffer is the buffer device's buffer, so `delayline` landed in dsp as a shared primitive rather than privately inside the delay (`b5c672b`). `ReadHead` deliberately doesn't know about playback rate or direction — the caller passes per-frame offset drift — which is what lets a fixed tap, a repitching tape delay, a reverse window and the buffer device's detached heads all be one type. 4-point cubic Hermite reads, because linear droops and aliases audibly off unity rate. Six controls didn't fit 1U, so faces became width-quantized in rack units.

Which immediately exposed that the rack's ScrollView viewport was the constant 758px — the width with an *empty* chain (`6a19b68`). Anything beyond it was laid out and unreachable, and since the viewport never exceeded the visible width, the view couldn't scroll at all. Wrong since the effect chain shipped; one filter already overflowed it. Bound to the row's intrinsic width, because a constant has to be re-derived every time a device is added, which is exactly how it drifted.

Gate, compressor and limiter share one `dynamics` module (`9aa91fb`) — three detectors and three sets of decibel plumbing would have been three chances to disagree. Detection is stereo-linked on the louder channel. The limiter has no lookahead on purpose: lookahead means latency, and with no plugin-delay compensation in the engine it would shift a limited channel against everything else.

The mixer arrived in three moves. Channels got a bank of buses to feed, with master as bus 0 and buses only routing to lower-numbered buses — acyclic by construction, so the render pass is a single descending sweep with no topological sort in the audio callback (`2c3ee4e`). Effect commands started addressing an `EffectTarget`, so one set of messages serves a channel's chain and a bus's, with the per-slot machinery extracted into a shared `EffectChain`. Pan law normalized to unity at centre while staying constant power, since a signal now crosses several pan stages and the raw law charged 3dB at each. Per-bus peaks publish through a small array of atomics instead of the event ring, holding the peak until the GUI's read clears it so a transient between frames still shows (`4520b4f`). Then the mixer pane, where clicking a strip's name plate points the device rack at that bus — putting a compressor across a group is the same gesture as putting one on a channel (`c679128`).

And then I took the lower-numbered rule back out (`c472721`). It was never an internal necessity; it made the user's mental model carry an implementation constraint. Whoever edits the graph topologically sorts it and the callback executes the schedule — cheap here because every bus owns a permanent buffer, so the whole schedule is a `[u8; MAX_BUSES]` permutation compiled by Kahn's algorithm with no allocation. Cycles are refused at the picker (greyed with a reason, not hidden), at the command boundary, and on load, where a cyclic file flattens to everything-to-master so it still opens and plays.

Third pass at the build problem, and the first one with the right cause (`df2cbce`, `6f1df1c`): an earlier attempt used `nice`, which adjusts CPU scheduling and is ignored entirely by memory allocation and page reclaim. The limit is concurrent linking, not CPU. Capped debug info workspace-wide with split-debuginfo (a test binary went ~450MB → 217MB) and dropped default jobs from six to three; eight, six and four had each hard-locked the compositor.

## Aug 23 — Poly synth and zoom

A polyphonic source: `DeviceKind::PolySynth` with a 16-voice pool, per-voice oscillator/envelope/filter/pan plus global LFO and portamento (`3f520e9`), then its UI face across Osc, Amp/Filter, Mod and Voice pages (`cb0d14c`). Generic device chain controls (`df94176`) and channel clipboard actions (`3e08096`).

Noticed I was clicking pitch zoom-in three times before every edit, so the default row height now starts there (`e685af3`). The four zoom buttons collapsed into one horizontal and one vertical `ZoomScrollBar` — thumb pans, end grip zooms around the fixed opposite end, built-in bars off so there's exactly one per axis. The widget itself was rescued from an abandoned selector branch where it was the only thing that hadn't already landed on main (`8665a4f`).

Latest: duplicate loop-boundary events prevented (`f332b7c`), device frame metering fixed and controls compacted (`8b3a9d2`).

## Aug 23 — The effect container earns its name

The device host was a UI contract with a matching engine shape, but three things said the container wasn't finished. The dry path blended against a latency-introducing device's wet copy with no alignment — drive reports 15 frames, so its own dry signal combed against itself. Bus effect slots were dead glass: `DeviceMeters` had no address space for them, and the rack polled the selected channel's chain even while editing a bus. And the trims read in percent of a 0..2 range, which put unity at "200%" and geared the drag wrong, while the source device's output trim sat at a frozen "80%" because the window property behind it was only ever restated on channel selection (`a82bed0`, `c58f3fb`).

The alignment fix is a stereo integer ring per slot (`DryAlign` in mooloop-dsp), built from the node's `latency_frames()` at install time and reclaimed with the node on removal. Bypassed slots keep feeding their ring so the delay history survives a bypass round-trip. Meters gained a target-index space: channels 0..256, buses above them, one polling loop for both.

Trims moved to dB from unity — −60 dB (−∞) to +12 dB, one `TrimKnob` class for the effect in/out, the rack-row volume and the source output trim, all storing linear gain in the wire and project file. Linear-in-dB needed no custom curve; dB is already the logarithm of gain. Channel volume widened to the same +12 dB ceiling so identical-looking knobs behave identically.

Then the faces (`a65bd0f`). Seven files each carried a copy of the header strip, the drag handle, and — on six of seven — a hidden rectangle of bypass/remove buttons that rendered nothing and wired nothing. All of it collapsed into `EffectDeviceShell`; a face now declares its name and unit count and its controls arrive as `@children` under the shell's header. Net −307 lines. A new effect kind is a face of controls plus DSP wired to the container; no chrome of its own to write or drift.

## Aug 23 — Piano roll opens up, the sampler catches up

A lint pass closed out the effect-container work (`8877380`): named-field struct literals in place of mutate-after-`default()` in the poly synth tests, and a `RemoveEffect` branch rewritten as `.map()` instead of an `if let`/`else None`.

The piano roll's pitch axis had quietly been capped at C2–C6 (`piano-low-note`/`piano-high-note` = 36/84, a 49-row canvas) since the zoom work landed — anything below C2 was simply unreachable, the same class of bug as the 758px rack viewport a day earlier: a constant sized for the common case with no way to reach past it. Widened to the full MIDI range 0–127, with every pixel extent (`piano-content-h`, the key-row loops, the zoom scrollbar's fraction math) re-derived from a new `piano-note-count` property instead of a second hardcoded `49` (`e45a37a`). The viewport now opens on C6 at the top so the working register doesn't move, but nothing stops the user scrolling past it.

That freed the vertical `ZoomScrollBar` for the pitch axis, so both zoom button pairs came out entirely: the horizontal bar under the grid already worked this way, and pinch-style `Ctrl`+wheel (`Shift` to target time instead of pitch) covers the case a scrollbar-drag doesn't (`f7e36a0`).

Then a full pass on the sampler, framed as its own session (`7e99e34`):

- A new channel no longer synthesizes a kick into its sample slot — it starts genuinely silent, `SampleReference::Empty` (`87ce723`). `Builtin { id }` stays only so a project saved before this change still sounds the way it did; the engine's default sample pool went from "every slot pre-loaded with a shared kick" to "every slot `None`."
- The waveform view gained the same `ZoomScrollBar` already proven on the piano roll's time axis, re-binning from the actual sample data for whatever range is visible rather than stretching a fixed 256-bin overview — the previous view was too coarse to place a loop point precisely (`79fe51f`).
- Start/End/Loop Start/Loop End became exact frame-index fields (`SampleField`, drag/scroll plus `Ctrl`/`Shift` for single-frame steps) next to the waveform markers, which were pixel-precision only (`e09ceca`).
- Coarse/Fine tune moved from full-width `ParameterFader`s to a `MiniKnob` pair — matching the mono/poly synths' own coarse/fine convention — freeing a row for a "C4 · 261.6 Hz" readout, since "+3 st / +40 ct" doesn't say what pitch that actually is (`e11328d`).
- A `PlayheadMeters` atomic array, shaped like the existing `BusMeters`/`DeviceMeters` (latest-value-only, no backlog), lets the audio thread publish each sampler voice's position once a block; the UI's 8ms pump timer draws one faint line per active voice, so layered retriggers show as several moving lines (`fa33fcd`).
- The new tune readout then got clipped at the bottom of the sampler's fixed 240px content budget — the knob column had grown taller than the fields column beside it. Dropped the redundant "Coarse"/"Fine" captions (the tooltip already names each knob) to bring the row back under budget, and extended the screenshot regression to actually stage sample frames, the tune label, and playhead positions so this class of clip gets caught next time (`ae7af0f`).

Last: the channel-settings save validator still checked volume against the old `0.0..=1.0` unity range from before trims moved to dB, so a channel legitimately turned up toward the new +12dB ceiling failed to save with a generic "invalid mixer values" error and no way to tell which field or why. Rewrote validation field-by-field — each check now names its parameter and reports the allowed range in the error, and the volume bound follows `MAX_LINEAR_GAIN` instead of a stale literal (`b1230c4`). README updated to say plainly that normal use can't fail a save anymore, and a malformed file identifies its exact channel/parameter/range in the error dialog instead (`38acde7`).

## Aug 24 — A real preferences dialog, then EQ, reverb, and modulation

Appearance prefs had been a single-purpose dialog; folding Audio settings into it as a second tab would have meant a second bespoke dialog shape. Rebuilt it as a general two-pane `PreferencesDialog` — a page nav list down the left, page content on the right — with Appearance as its first page (`76634ce`). JACK device controls landed as the second: driver select (JACK only, ALSA later), an output-device picker read from the live JACK port graph, buffer size, a read-only sample-rate readout, and auto-reconnect for when the port graph changes underneath the app. The JACK surface is its own Slint component rather than a driver-agnostic one, because JACK's client API has no sample-rate setter and buffer size is server-wide, not per-client — a future ALSA page is a sibling, not a rewrite, since the two drivers expose genuinely different controls (`35b4084`). Settled a few days of build/doc loose ends alongside it: portable Cargo target output, linker requirements documented, dev setup instructions trimmed (`4d36c04`, `9d94853`, `ee8a1fd`), and a later pass tightened the dialog's own layout now that it had two real pages to balance (`73aca0f`).

Two of today's doc commits are process, not content: an `AGENTS.md` exception letting an untracked Markdown file under `docs/` be committed alongside doc work without asking each time (`fdfff4e`), and preserving a couple of Adam's own working notes — dockable-pane and step-shading ideas for `ENHANCEMENTS.md`, a `SHORTCUTS.md` stub (since consumed, now `docs/archive/SHORTCUTS.md`) — that had been sitting uncommitted (`37f46d3`). Also today: the journal's own do-not-edit guard came off, since keeping it updated live turned out to be fine (see the note at the top of this file — no commit hash, just Adam's call).

The effect suite grew three devices in one day. Seven-band parametric EQ: peak/shelf/pass biquads per band plus dedicated high/low-pass filters, sample-timed coefficient updates, no allocation in `process` (`4a7cf6f`). Then a compact allocation-free `SpectrumAnalyzer` — a fixed Goertzel bank over a mono sum, 48 log-spaced bands, computed once per 2048-sample hop and only when a device display subscribes — so a device host can show a spectrum without ever handing PCM to the UI thread (`f6ffc1a`). Convolution reverb followed: an IR player first, a room generator second, meeting at one `StereoIr` boundary so a future WAV/AIFF loader is just another producer of the same type. Rooms combine a small image-source early-reflection model (shape controls reflection density) with a material-filtered deterministic diffuse tail; the direct sound is deliberately absent since the host already owns dry/wet blending (`385a765`). Preparing a room's FFT partitions runs off the audio thread on a worker that coalesces control edits for 80ms, and the resulting prepared resource carries a fingerprint so a slot refuses a stale swap — a generic mechanism meant to serve future resource-backed devices, not just reverb (`16c7b48`). And a unified modulation effect: chorus, flange, ensemble, and ADT as policies over one short fractional stereo delay line, plus a phaser sharing the same LFO and parameter contract even though its all-pass cascade is a different signal path underneath — one device rather than four, because a user thinks of them as one knob-turn apart (`76c8730`).

Then the CPU jankiness Adam had been noticing predated all of this and finally got a real diagnosis: recursive DSP state — filter feedback, envelope followers, parameter smoothers — decays asymptotically toward zero and lingers in subnormal float range on the way there, and unflushed subnormal arithmetic can be an order of magnitude slower on x86. That reads as constant, unattributable load rather than one hot function, which is exactly what made it hard to pin down. Fixed at the source: set MXCSR's FTZ/DAZ bits once per realtime callback (MXCSR is per-thread, so this can't happen once at construction), with snap-to-zero epsilons in `Smoothed` and `EnvelopeFollower` as defense in depth so correctness doesn't depend on the hardware flag alone (`0921c85`).

## Aug 24 — A plans workflow, and paying down what it found

Also today: `docs/ARCHITECTURE.md` plus a mermaid diagram map the application's crates and control flow at a glance, separate from the detailed audio-core contract (`8fb8bd3`). And a new habit — dated, numbered plan documents under `docs/plans/<topic>/NN-step.md`, moved to `docs/plans/archive/` once done — started today with `reduce-ui-pump-overhead`, `amortize-reverb-partition-cost`, `auto-offline-idle-devices`, `extract-mid-level-dsp-blocks`, `reduce-audio-jack-buffer-size`, and `share-dsp-primitives` (`1818169`, `029d2b0`, `5cbfcfe`). Several landed the same day they were written:

- The UI pump was invalidating Slint state it hadn't actually changed: a meter redraw fired on every ballistics tick even when the lit-segment count was unchanged, and the document title got rewritten on every transport command instead of once when dirty state actually flipped. Both now compare against what's already displayed before writing (`735270e`), then the plan itself moved to `archive/` (`ab8709e`).
- `EffectChain`'s realtime pass looped over all `MAX_EFFECTS_PER_CHANNEL` slots per channel and per bus regardless of how many were actually populated. Added a `bound` — one past the highest occupied slot, maintained incrementally on install/remove/swap rather than rescanned every block — so the render cost is proportional to the chain that's actually there (`1fd6a3c`).
- JACK's buffer size had never been set explicitly, so the engine just inherited whatever quantum PipeWire's JACK shim happened to be running — 1024 frames / ~21ms on this workstation, which read as generic sluggishness with no attributable cause. Defaulted to 256 frames as a safe middle ground; 128 becomes reasonable once the render-cost plans above land fully (`f33bd28`).

Then the parameter-smoothing pass `share-dsp-primitives` opens with: `Smoothed` existed and only `MonoSynth`/`PolySynth` used it, so every continuous effect parameter stepped the signal at each block boundary instead of ramping — zipper noise on a filter sweep, a click on a drive/delay/dynamics gain change. Wrapped the audible continuous parameters per effect (filter cutoff/resonance, drive's four controls, delay's feedback and damping coefficient, modulation's depth/feedback/spread/tone/color, the compressor and limiter's gain-shaping controls), left the genuinely discrete ones alone (bitcrush's bit depth and downsample rate — the steppiness *is* the effect), and added a discontinuity regression test per changed effect so a future effect can't skip smoothing silently. EQ and reverb stayed deliberately unchanged, with the reason recorded in-file rather than left implicit (`41bf343`).

That plan's second step swept the codebase for filters that were the same primitive under different names — confirmed by reading each site, not assumed. `OnePoleLp` (matching the existing `OnePoleHp`'s shape), `AllPass` promoted out of modulation's private copy, `Biquad` plus its RBJ-cookbook designers promoted out of EQ, and a shared `scale::hz_from_normalized` for the `20 * (max/20)^x` log-frequency mapping duplicated across four DSP files — landed unused first so a bisect could separate "the primitive is wrong" from "the adoption is wrong" (`6f5a4e4`), then adopted one call site at a time: `OnePoleLp` gained a precomputed-coefficient constructor for delay's damping, which smooths a coefficient directly rather than a cutoff-Hz control (`4e4f541`), all three one-pole tone/damping filters switched over bit-identical (`6e8af0b`), the four log-frequency call sites collapsed onto the shared helper (`11241e1`), and modulation's phaser/chorus swapped its hand-rolled all-pass and LFO for the shared ones — the LFO swap needed a new `Lfo::peek_offset` to read a second tap of the same cycle at a runtime-variable offset without disturbing the accumulator, since stereo spread is smoothed continuously now rather than fixed at construction (`4fd91e5`, `56e9ada`). Adopting the smoothed cutoff moved a test's assumptions with it: a filter-bypass test measured energy in the same block a cutoff change was queued in, which only worked while cutoff snapped instantly — now it renders one block to let the new 3ms ramp settle before measuring (`ce32d17`).

The plan's third step was explicitly to *attempt* collapsing the sampler's own inline SVF and ADSR onto the shared `crate::filter::Svf` and `crate::env::Adsr`, then measure and decide rather than trust the existing comments. Both stay separate, for sharper reasons than before: the shared `Svf` recomputes its coefficients (including a `tan()`) every call, while the sampler computes once per stereo frame and ticks both channels — calling it per-channel would double that cost, and a synthetic benchmark confirmed roughly 2x, consistent across runs. The shared `Adsr` preserves envelope level on retrigger by design (avoids a click when a synth voice retriggers over its own release tail); the sampler's `trigger()` always hard-resets on a voice-steal, because restarting a sample from its start is the intended behavior for a stolen voice, not a legato retrigger — adopting `Adsr` as-is would have silently changed that. No code changed, only the header comments, which now state the measured/reasoned cause instead of just the fact (`5d82121`).

Last two of the day: both the EQ band handle and the reverb capture-point dot re-centered under the pointer mid-drag because their position was bound to the value it controlled, turning the drag into a sign-flipping recurrence — the same pitfall already worked around on the mixer fader. Switched both to the same delta-from-press-point pattern (`f0da9d5`). And keyboard shortcuts became reassignable: a named `ActionSpec`/`KeyChord`/`ShortcutTable` registry that transport, file, edit, view, channel, and pattern operations all dispatch through, so a future console or MCP server has one seam to hang off instead of each growing bespoke wiring; `main.slint`'s key handling collapsed from a hardcoded chain to generic decode-and-dispatch, and a Preferences > Shortcuts page does (re)binding with conflict handling. Manual testing caught a real bug along the way: a bare Ctrl keypress reports the same key code as Ctrl+Q in Slint's key model, so every Ctrl-chorded shortcut was misfiring quit until standalone modifier presses were explicitly rejected (`faa66eb`).

## Aug 25 — The buffer device, and a queue for the effects

The retained-audio buffer device — the thing `docs/BUFFER_ENGINE.md` had described since Aug 18 as what the whole instrument is eventually for — became real in seven commits. Core ring and read heads (`2a0a814`), the engine insert (`298c5ee`), off-thread ring resize because changing the length reallocates (`6fff8dd`), collision telemetry and the acceptance tests that go with it (`e0bf382`), a debug trigger surface on the face (`6869acc`), an insert-menu entry (`055ac02`), and reverse as a *held, looping* gesture rather than a one-shot (`2f5f8ce`). The load-bearing rule is that Follow is not "minimal latency" but zero: write the block into the ring, read from the position just written, so bypassing the device is sample-identical to running it. No safety margin.

It needed something to fire events at it, which produced MIDI input and a buffer control mapping (`ea59b7a`) — a real MIDI port rather than a debug button, which is also the first thing in the project a hardware controller can touch.

Then `ParamAddr`, the modulation matrix, and an LFO source (`b576ad1`). `MODULATION_PLAN.md` had deferred `ParamAddr` until modulators gave it a second and third target; this is that pass, and three decisions in it survived everything since. The address carries its channel or bus from the day it exists, so cross-channel modulation later is a routing change rather than a retyping of every engine command. The matrix keeps depth per route and leaves the base value alone, so a knob and an LFO cannot fight over one number — re-assigning a source to a destination it already drives *retunes that row* instead of stacking a second one, which is what the drag-to-set-depth gesture does every time it is used. And evaluation runs at a 32-frame control rate: once per block stair-steps audibly on a fast LFO, per sample buys nothing at these rates. Channel LFOs reached effect parameters the same day (`9323f7e`).

Also: Adam wrote `docs/archive/EFFECTS_FEEDBACK.md` (`54c8bb6`) and it became a queue of fourteen numbered plans (`68bd226`) rather than a list of grievances — four of them landed immediately (`984d08b`, `0c5db0f`, `9a5f204`, `4029909`), including one shared `DraggablePoint` for EQ and Filter instead of two bespoke `TouchArea`s. The agent worktree workflow became mandatory rather than advisory (`9355c24`), and packaging learned to build RPMs on demand and fetch them back (`0e6afa4`, `99e6f09`, `43fab6c`).

## Aug 26 — Automation lanes, and generators join the parameter system

An automation lane is a sorted list of normalized breakpoints aimed at a `ParamAddr` (`7b12478`). It stores 0..1 rather than natural units so it survives a descriptor's range changing under it, and so it speaks the same form the knob and the matrix already do. The important part is what happens when both address one destination: **a lane supplies the base the knob would otherwise supply, and the matrix adds its offsets on top**, so an LFO wobbles around a drawn curve instead of one of them winning. Both emit at the same 32-frame control rate into the destination's existing event list, so no effect needed a change to receive them. The piano roll grew velocity and automation lanes to draw them in (`c7e419b`), and the buffer's read head became automatable (`532a666`).

Which immediately exposed that lanes worked on half the project. Effects had been `(target, slot, param_id)` since parameters got ranges; generators ship whole structs, so nothing inside one could be named. `GeneratorParams` mirrors `EffectParams` exactly — same descriptor type, same never-renumber rule, same clamping `get`/`set` — and the three-oscillator synths reserve ten ids per oscillator starting at 100 so a fourth parameter can be appended later without renumbering a shipped id some saved lane is holding (`2d1623b`, `50847d6`).

**The drum synth was left deliberately empty rather than partly covered**, on the grounds that a half-addressable device is worse than an honestly unaddressable one: the picker would list some of its knobs and silently omit the rest. That call was correct, and it is also the seed of the DS-01 plan a week later — the reason it cannot be fixed by writing the table out is that `DrumSynthParams` is a mode-union, so the ids would mean different things in Kick and Hat.

Three effect-feedback plans landed alongside (`1793735`, `714ad53`, `3c55905`), the last shrinking EQ to 2U with a selection strip.

## Aug 27 — Release copy, and three colour seeds

Short day. The release README was rewritten twice (`57c9a9c`, `9beb603`) and AppImage packaging got the `file` it was missing (`94a8557`). Preferences > Appearance was rebuilt on three colour seeds (`5186f2c`) rather than a flat palette list, and Adam fixed layout issues on both synths and Plate himself (`7cab9e9`).

## Aug 28 — Docks, the browser, and the gain contract

The window learned to hold panes: an always-visible docked status bar (`a8def47`), a draggable splitter for the piano roll dock (`29a0d55`), and a browser sidebar shell on the right (`8ae6ffe`) that became a real sample browser (`5c9dd8c`) with autoplay previews, volume, and an info pane (`6fbc974`).

Then the gain contract, planned and executed in one day across eight steps (`80ce9cd` → `f85fc69`). The order was the point: **measure first** (`4ab5cf8`), so every later number could be compared against something. What the measurement found was that sources sat between -0.2 and -8.1 dBFS at unity — no shared reference at all — that the mixer fader's travel *was* linear gain, so three-quarter travel meant -2.5 dB, and that reverb's wet path ran 9.7 dB above the dry signal it blended against, with Plate at 11.7.

One shared `gain` module in `mooloop-core`, mirrored into Slint as a `GainMath` global with a test that parses the taper table out of the `.slint` and fails if the two drift (`2b01c34`). Faders moved to a console taper (`22ca712`). Every source was calibrated to a -12 dBFS operating level (`cdd5450`), which put all four within a dB of each other and left real headroom — eight channels at unity now sum to +6 rather than +13. Drive stopped peak-normalizing and started normalizing by the shaper's response at the operating level (`d2e490d`), so raising drive grows harmonics instead of making a signal up to 24 dB louder. Wet paths were level-matched with an equal-power host blend (`c529dd2`), meters got a -10 dBFS warning threshold (`0fe6dab`) and IEC 60268-18 ballistics (`97bb82e`).

Three tests written in the same pass are worth naming because they pin an architectural claim rather than a value: that the summing path cannot be level-dependent, that effect *placement* rather than the effect is what couples tracks, and that the bus-to-bus hop obeys superposition too (`e542c7f`, `de9d9bc`, `a9a96e6`).

Also today: the channel modulator system was specified (`9ffcf4a`), its metadata became load-bearing rather than decorative (`9a34978`, `c32cc40`), the modulation shelf appeared (`c13c872`), Mono v2 and Poly v2 were planned as two separate instruments rather than one shared refactor (`1dc081a`), and heavy builds moved to the remote box (`92373f8`).

## Aug 29 — Modulation becomes visible

The shelf was laid out properly and started routing by descriptor id rather than by position (`2f3434f`), and source assignment was separated from source selection as an explicit mode (`4214926`). The LFO grew free and synced rates, fade-in, pulse width, and note-triggered reset (`2a3f35e`); **every device knob in the app became an assignment target** (`9952704`); and the knob itself learned to show that it is assigned and to move while a modulator drives it (`ff507d3`) — which is the difference between modulation existing and modulation being usable. A gate-driven envelope joined the LFO as the second source kind (`3c80ed8`), with an explicit cross-channel piano-roll gate input.

Beside it: a central `Motion` global with animated pane hide/show (`4376886`), status-bar polish and a bottom-pane toggle (`14ac368`), common sample audio formats (`fc4fa2a`), and notes finally spanning the full MIDI range in every code path (`ac92add`).

## Aug 30 — A real reverb, and the ML-1 becomes an instrument

The convolution reverb was replaced by a feedback delay network (`71d1d70`), and the commit is a good example of a plan being overtaken by a better answer. `docs/plans/archive/amortize-reverb-partition-cost/` asked for the partition MACs to be spread across JACK blocks. That would have fixed one of three problems. Cost: all partitions accumulated in the single call where the 512-sample window filled, so a 2 s tail spent ~1400us in one block against a 1333us budget — the network is flat, 31x smaller at worst case, and no longer varies with decay at all (0.5 s and 20 s tails both measure ~13.6us mean). Modulation: **a convolution node cannot take a parameter event**, so every route aimed at a reverb knob had been a silent no-op while the metadata declared all six legal. And a 20 s tail was not previously reachable at any cost.

Then the v2 mono synth, built as a *third* device so the v1 stays loadable until its channels have somewhere to go (`520fa91`, `dc3380c`, `02c3add`), named the ML-1 (`3cd4afd`), and given the thing that makes it an instrument rather than a Poly with one voice: **the filter becomes the star**. A four-pole ladder with saturation ahead of it (`984d2c2`), where stability is structural — the stages only ever integrate a `tanh` output, so every stage is bounded by 1 whatever resonance, cutoff or input level do. Two constants there are load-bearing and both were measured: one puts the corner where the SVF's is so the Cutoff knob means the same frequency on either, and one scales feedback with the stage coefficient, without which the filter self-oscillates at 200 Hz while merely peaking at 2 kHz.

Adam then lifted the plan's one-filter restriction, and three characters landed behind one Model switch (`e07aca8`). The rule that survived is the one that matters: no LP/BP/HP response menu, because that would make the ML-1 the generic multimode synth Poly already is. All three are low-pass; they differ in slope, in where the nonlinearity sits, and in what resonance does to the low end. Acid is a genuinely different circuit — three poles, asymmetric saturation through the tape curve so it generates even harmonics too, and a quarter of the ladder's input bypass so resonance squeezes the body out of a note. Accent arrived as a depth control on velocity (`97b311d`), the shared voice conventions were separated out (`4853ad0`), and a factory bank shipped along with the mechanism to ship one (`0f655ea`).

Also: sampler markers learned to snap to zero crossings (`6b647cb`, `07c28e0`, `7fd5820`), bitcrush got four crusher algorithms (`d379448`), dynamics got a draggable threshold on a real input display (`240e28f`), and Poly v2 was redesigned as ML-P8 (`faa7d00`).

## Aug 31 — The piano roll grows up, and documents stop refusing to save

The piano roll had one gesture set and no way to build a selection with the mouse at all — multi-select existed in the model and was reachable one Shift-click at a time. Five pointer tools, a marquee, and an independent snap toggle fixed that (`cb56aae`), and then a selection became **one object**: a left edge that had never existed, length and resize applying the anchor's delta across the group and clamping by the group rather than per note (which would quietly flatten a chord's rhythm at the limit), and time-scaling a phrase (`79c1b95`, `423f8c8`, `26389c1`). The three gestures now share one "which notes does this act on" helper, which fixed a case the move handler had wrong. Drag modifiers became remappable (`9e9217d`), one drag became one undo entry (`3fb83cf`), and the note canvas was lifted into its own component (`93c8b68`).

Saving stopped refusing documents (`75383cb`). It had been stopping at the first failed range check and returning one sentence with no location — "invalid document: channel 3 has invalid sampler parameters" — and the same checks guarded loading, which is worse: a song already written in that shape could not be opened at all. Validation is now one traversal that **detects and corrects together**, each check a single arm carrying both the problem and the correction so a range and its clamp cannot drift apart. Save and load run it in repair mode. The app also learned to log what it does and to hold on to a song it cannot save (`de26864`).

The sampler got band-limited playback (`171f0d1`) — a windowed-sinc reader where unity rate is sample-*exact* rather than merely close, which is what made it safe to drop under existing projects — plus its own filter envelope (`fa10b37`) and an output trim that lands a loaded sample at the operating level (`5a4d30c`).

And the ML-1 was corrected to **ML-M1** (`8beb42b`), the name it was always meant to have. Three on-disk identifiers keep the old `ml1` spelling deliberately and must not be "fixed": a serialized name is an on-disk identifier like a parameter id. Adam played the factory bank the same day and reported it sounds very good, with one note — the filter models had noticeably different apparent loudness. Measured at 10.8 dB worst case, it turned out to be two defects, one of which was fixed (`ac9fd07`): the nonlinear models *lose* level as resonance rises because their stages only integrate a bounded shaper output, while the linear SVF *gains* it from its resonant peak, so the three diverge as the knob comes up rather than sitting at fixed offsets. Neither existing guard could see it — one asserted only a peak ceiling, the other measured the step at the instant of switching, and a steady 10 dB offset passes both.

## Sep 1 — Modulator modules, capacity, and the stretch engine

Three workstreams in one day, 46 commits.

**Modulation became a module system.** Modulator parameters got the effect descriptor contract, collapsing the shelf onto one `param-changed` verb for a net −351 lines (`8ec97a8`, `c6f32eb`). Step, random, and math kinds joined LFO and envelope (`2c57d66`), each a descriptor table plus a tick. Then the shelf became a grid of eight (`f90d36c`, `626264f`) with durable `ModSourceId`s, so reordering moves modules without changing what any route means (`e749cf9`). Two findings worth keeping: the slot-order rule for the math module needed no machinery at all, because `outputs` already holds last tick's value everywhere the evaluation pass has not reached — so a module reading a lower slot sees this tick and one reading itself or higher sees the previous, for free, with self-reference bounded by the module's own output clamp rather than by a cycle check. And the reorder hazard was not the routes, which resolve by identity, but the math module's `input_slot` — a slot reference the user never sees.

**Capacity stopped being a memory argument.** The plan's first version measured only the modulator arrays, put the graph at 3.1 MiB, and named control outputs as the biggest line. Measuring the whole render graph said otherwise: 42.8 MiB, of which `ChannelStrip` was 37.7. Boxing the effect slot state took it to 11.6 MiB; materializing channels from the project instead of reserving 256 of everything took a sixteen-channel project to **1.1 MiB**; and narrowing modulation edits so each names one fact took `EngineCommand` from 936 to 136 bytes, which takes the command ring out of the capacity table altogether (`ffbaa98` → `5554cce`). Both ceilings are untouched; the graph simply stopped paying for them in advance. The wide `SetChannelModulation` variant was deleted rather than kept, because project load, presets and undo all rebuild through `install_project` and a dead wide variant would have held the ring at 936 bytes on its own.

**The sampler got a time-stretch engine.** WSOLA, ported from the #32 spike, reading through the band-limited reader's own `Region` rather than carrying a second opinion about where a loop wraps (`e530062`). Then the decision that shapes the whole feature: Adam's use for it is extreme slow-down and glitch pitch-up, and the spike had graded ratio 2.0 as "falls apart" because its onset detector lost ten of nineteen hits. **That breakdown is the sound.** So the ceiling is 16, not the clean band's 1.5, and the clean band became something the UI marks rather than something the DSP refuses. Grain mode is the third mode and its entire definition is that it does *not* search for the splice point — the search is what places every join phase-coherently, and without it each join is a discontinuity at a fixed rate, which is the buzz (`446a8b0`). Fit-to-tempo derives the ratio instead of setting it, with playback rate in the numerator, so transposing a voice up an octave doubles the ratio and the loop still lands on the bar — pitch stops being a speed control (`6bfb447`).

ML-P8 landed its parameter model and own id space (`da4a658`) and its oscillator network, sub and noise (`f2de216`). The sync BLEP made aliasing *worse* until two mistakes were fixed — the step height has to be measured on the naive waveform, and the oscillator's own cycle-boundary residual has to stand down for the sample after a reset — and neither shows up in a test that looks for energy in a high band, because a hard-synced oscillator is exactly periodic at its master's rate and every alias product folds back onto the master's own harmonic grid.

## Sep 2 — Slices, a widget audit, and the addressing bug

The sampler's slice work closed out: a slice model with stable marker ids and serde defaults so old projects load byte-identically (`f41aea7`), a slice per note played backwards if asked (`a42e9aa`), and the resolution to the rule the stretch work inherited from the spike. WSOLA runs forwards only, so reverse was refused while stretching and slice mode would have inherited that refusal. Rather than make the stretcher run backwards, **freeze it**: commit the stretch to the buffer, after which it is ordinary audio and reverse and slicing are ordinary sampler behaviour again (`46a759f`, `15345fe`). One finding there is a good illustration of why a second copy of an answer is a liability — the plan expected markers to be traced using the stretcher's `analysis_pos`, which is the production frontier and runs up to a hop ahead of what is sounding, putting markers 2.8 ms early at ratio 2.5. Driving the render through the reader instead means the commit's marker mapping and the live stretch's playhead cannot disagree.

The UI tooling had a clear-out. The mockup tool collapsed onto one catalog (`79b57a0`) and started stocking its palette from its own audit list (`179d183`); the control gallery was retired (`60e4d4d`) and two stale fixtures dropped (`bb7db8b`, `9401988`). `docs/WIDGET_INVENTORY.md` (`4f4957d`) records the complement of the mockup tool's UNCATALOGUED group — patterns that recur but have no component behind them — with counts measured rather than estimated. The headline is `PolylinePlot`: Slint's `Path` elements are static children rather than a model, so nobody can express one path over a series, and the one-element-per-sample-pair workaround is hand-rolled **17 times across 8 files**. `scripts/slint-sketch` (`b87d51b`) type-checks a scratch `.slint` against the real widgets in about 0.05s and screenshots it in about 0.2s, against roughly four minutes for `cargo build -p mooloop-ui`.

ML-P8's face became a network drawn as a network (`0b57044`), and then Adam marked it up — purple where space was used and not needed, orange where it was awkwardly empty, plus the note that it still read as a stereotypical Linux UI. Both colours had one cause and the fix was an idea rather than a nudge: **a mix level is a route** (`249ff65`). "This source into the output" is the same kind of fact as "this source into that oscillator", so it became a MIX column of the grid, separated from the route columns by a rule. That one move fixed both marks — the matrix's empty lower half became five sources by four destinations, and two columns lost rows. The three bordered panels became one surface divided by hairlines, because three identical rounded cards floating on a background is the look of a settings dialog and was most of what made it read as generic.

DS-01 was planned end to end (`c1e3a3c`, `ab1b3d7`, `d345a53`) with three face concepts rendered at the real face size against the real widgets — two of them built, rejected, and checked in as the argument for the third. The adopted layout carries a consequence the others had hidden: **DS-01's controls do not fit on one face unless the scopes are the envelope editor**, with times as handles dragged on the curve rather than knobs. The Mod device shrank from 3U to 2U and was recomposed as an instrument panel (`87d18f8`, `5013ad9`), closing the last of the fourteen effects-feedback plans.

And then the bug of the week. **Modulation routes and automation lanes named their destination by effect slot and their channel by index**, so any structural edit silently re-aimed them: reorder a rack and the LFO drawn on a filter's cutoff started driving whatever slid into that slot while the filter went dry; delete a channel from the middle and every later channel's routes went inert while its lanes started automating the channel that moved into their seat. `mooloop_core::structure` now states each structural edit **once** as a permutation — `SlotRemap` for a chain, `ChannelEdit` for the channel list — and runs it over everything that stores a position: the matrix, every lane in every pattern, and the lane the editor is showing. The UI model and the engine apply the same table for the same command, so neither can point at a different device than the other. `SwapEffectSlots` became `MoveEffect`, a removed device takes its routes and lanes with it, and the integrity pass repairs what earlier versions left in saved songs (`0c60828`). Effect add, move and remove became undoable in the same commit; they were the only rack edits that were not.

A follow-up closed the identical hole one level down (`c1765e9`): `ParamOwner::Modulator { slot }` is a position too, so reordering the grid left a route whose *destination* was a modulator parameter pointing at the old slot, and emptying a slot dropped the routes that module drove without dropping the ones that drove it. Nothing authors such a route yet — which is why neither showed up as a symptom, and why wiring it now means the day something does, the grid's reorder does not silently re-aim it. Three smaller things fell out of the same pass: the lane retarget was walking all 256 preallocated patterns on the realtime command drain rather than the song's own; the integrity pass was judging a route against its generator's descriptor table even when that table is empty, so a route naming a drum synth control would have been deleted rather than kept for the day it gets a table; and an assignment onto a full matrix returned false and did nothing visible, which reads as a broken knob.

## Sep 3 — The ML-P8 gets modulation of its own

Step 04 of `docs/plans/poly-synth-v2/` had landed as parts: the LFO and the route compiler were written, tested and completely unread, every item carrying a dead-code allow. Wiring them into the sound is where the decisions were, and four of them are worth keeping.

**A route offset goes on before the curve, not after.** The map from authored percent to phase deviation squares the magnitude, so adding an offset to the already-curved value would make an amount of 20% move a different distance at every point on the knob. `Prepared` now keeps the authored percent beside the prepared cycles for exactly the amounts a route can reach. The same reasoning split each oscillator's semitones from its cents, so a route reaching pitch is clamped through the semitone control's own range and cents stay the fine offset the patch authored — and it is why the sub follows its source's *resolved* pitch: it divides an oscillator, so reading the authored ratio would leave the fundamental behind the instant a route moved the oscillator it comes from.

**A route at zero amount keeps its compiled row.** Dropping it is the obvious optimisation and it quietly breaks the step's own promise that automating an amount never rebuilds the topology, because a lane sweeping up from silence needs a row that is not there. The same question came back a second time from the other side: once an amount has been automated away from what the parameter block still carries, every later knob change *looks* like a structural edit, so the node compares route topology rather than equality and retunes the rows it already has.

**The block-level skips had to learn about routes.** "Nothing reads this oscillator" was decided from the authored levels and amounts, which was true until a route could move them. Every one of those skips — the oscillator, the noise, and the whole filter bypass — now stands down when a route exists. Left alone, a route would have been silenced by the optimisation it invalidated, which is a bug that only appears in the patches the feature is for.

**A route amount had nowhere honest to be addressed from.** `ParamAddr` names a device and a descriptor id, and a depth is neither: it belongs to the route, and the route belongs to the patch. Carving sixteen routes' worth of ids out of the device's own table would have spent a permanent block on a capacity number the plan calls provisional, and pinned every route to the slot it was authored in. `ParamOwner::SourceRoute { route }` is the address instead, and the cost is four bytes on every stored `ParamAddr` — measured, argued in the footprint test, and not paid on the command ring, which the rack stopped travelling on.

The face became two pages. That cost twenty-four pixels of a face that was already full, and paying it out of the network grid was not on offer, so it came out of the VOICE region's knob diameters and its three displays. Four knobs across was tried first and reverted: it fit the height and clipped at a 960px window, which is the width this region loses first. `SyncMiniKnob` and the tempo-division vocabulary moved out of the modulation shelf into `controls.slint` on the way — the ML-P8's rate is their second caller, which is what makes a private component's generality real rather than assumed.

One thing the plan asked for and did not get: a per-route polarity control. Polarity is a property of the *source* in the plan's own table — the LFO and Key swing both ways, an envelope and velocity rest at zero — and the sign of the amount already decides which direction a unipolar source travels. A switch would have been a second answer to a settled question, so the row shows it as a badge.

## Sep 4 — The ML-P8's pool becomes a pool of groups

Step 05: Unison, Detune, Spread, Drift, and a finishing chorus. The plan's own framing is that none of them may carry the instrument's identity, and the acceptance case is 1x, Drift 0, Chorus OFF — so the interesting part was making sure they cost nothing when they are off, which is where the device now ships.

**A note stopped being a voice and became a group, and `age` was already the identity it needed.** Every member of a group is stamped with the same age when it is allocated, so "the oldest age still on the board" names a whole group and stealing needs no second table. The event id could not have done it: it identifies the note, and a slot that is being retired has stopped being part of one. What the group model then turned up is that stealing can be *asymmetric* — a patch that was at 8x when a note started and is at 2x now steals eight slots to fill two, and the six left over have nothing to adopt. Dropping them is exactly the one sample of silence in the middle of a sound that stealing exists to avoid, so they leave through the release with their age set to zero, first in line to be reused.

**"Drift 0 is exactly authored" is an identity, and the code has to earn it rather than test for it.** Every multiplier Drift introduces is written so that it is precisely `1.0` at zero — `exp2(0.0)` is one, and `x * 1.0` is `x` bit for bit — instead of being skipped behind a branch. That is what let the drifted pitch fold into the oscillator ratio the sub already reads, rather than becoming a second multiply the sub would have needed its own copy of; and it is why the test for it is a byte comparison of two renders instead of a tolerance.

**The finisher's buffers were the expensive part, and the footprint test is what asked.** The chorus may not read the channel bus — that is the plan's rule, and it is what stops it rewriting audio another generator already put there — so it needs two of its own. At `MAX_BLOCK_SIZE` that is 128 KB on every materialized channel, for a control that is off by default on an instrument that is not even the active generator on most of them. Rendering the range in 512-frame chunks makes it 8 KB and costs one `Prepared` per chunk, which is nanoseconds against ten milliseconds of audio. Nothing in the device is per block, so the chunk boundary is not audible, and the bit-identity tests are what say so rather than a paragraph.

**A mode change is a topology change, so it happens in silence.** OFF and the three live modes swap the shared `ModulationEffect` underneath, which cannot be done under running audio without a step. The wet gain runs to zero, the swap and the delay line's clear happen there, and the gain runs back up — which makes OFF reachable by the same path as every other mode and makes it a true bypass once it arrives, rather than a mode that happens to be quiet.

The face had no room, and finding that out took three tries. The VOICE region was full at its real 240px height, so the block drafted as a band under the region became two more columns beside the knobs. Then the snapshot test said the new controls "drew nothing" — and the reason was that a source face is a fixed number of rack units, not a fraction of the window. Sketching the *unmodified* face at its actual 664px showed it fitting exactly, with no slack anywhere: three units was not a comfortable width it had been given, it was the width it had been cut to. So the face is four units now, on the same argument DS-01's five already stand on, and the columns are narrower than the two beside them so the margin at four units is twenty pixels rather than two. `VOICES 8` reads as instrument information rather than as a control, since nothing on the face can change the pool; the number that moves is the derived one beside the selector, and Rust supplies it so the arithmetic lives with the allocator instead of being restated in markup.

The general lesson is the cheap one: `scripts/slint-sketch` was answering a question I had not asked it. Rendering the new layout at a plausible width proved it *could* fit; rendering the *old* layout at the real width was what said there was nothing to fit it into.

The plan asks the step to stay inside the realtime budget, and nothing in this repository measures one — so it is a measurement rather than a test, recorded in the plan status. The worst case the step can build, on a release build on the box, is 12.2% of one core; the Ensemble chorus takes it to 13.2%. A wall-clock assertion in the suite would be flaky on the laptop and would fail for reasons that have nothing to do with this device.

---

## Sep 4 (later) — a device's outlets become something you can route

Step 06 of `docs/plans/poly-synth-v2/`, in two sittings: the vocabulary first, then the mechanism.

**An outlet is not a modulator, and the route type had to admit it.** `ModRoute` stored a `ModSourceId` — a rack module's durable identity, minted when the module is added and resolved against the rack's slot table. A generator outlet has none of that: nothing mints it, nothing reorders it, and it belongs to whichever generator the channel currently holds rather than to the rack. So the route's source became a `ModSourceRef`, which `mod_metadata.rs` has been shipping unconsumed since the spec landed, for exactly this. Four bytes a route, and the same argument `ParamAddr` already made when it grew four to carry a durable route id.

**The one-block latency turned out to be an ordering fact rather than a delay.** The control table is filled before the strips render. So what a route reads is *necessarily* what the generator published at the end of the previous block — there is nothing to schedule, and nothing that can forget to. That is what makes an offline render agree with a live take, and it is why the test for it asserts the cutoff is still at its base in the block the note lands in.

**The two halves of the control space are captured at different rates, and pretending otherwise cost 8 KB a live channel.** The first shape widened the per-tick control table to hold the outlet band, which stored eight block-constant values in all 256 tick rows. The footprint test caught it, for the second time in two steps — the first was step 05's chorus scratch. `ControlSources` is the fix: routes and projects still see one flat address space, because that is the part they depend on, while each half is stored at the rate it is actually captured. The cost of routable outlets came down from 8,224 bytes a live channel to 32.

That test has now paid for itself twice in a day, and both times the same way: not by finding a bug, but by turning "this seems fine" into a number somebody has to justify in a comment.

## Sep 4 (later still) — the ML-P8's face stops being one screen

Adam played the shipped face on a 14" laptop: "the controls are small and hard to see... might be ok on a 24 inch screen but not 14". He is right, and the number says why. ML-P8 has sixty-nine parameters and one 884x240 face, and the only way that ever fit was a 20px dial under a 9px caption. **A face that fits by shrinking its controls has not fit.** It is five pages now, at a 34px dial, and the face is exactly as wide as it was — pages bought the size, not width.

**The rejected layout and the shipped one were the same failure from opposite directions.** `docs/plans/drum-synth-v2/mockups` records that ML-P8's *first* face was rejected for being "pages of knob rows", twenty-six near-identical small knobs per page. What shipped instead was one screen of small knobs. Both are the same mistake — controls sized by what was left over — and the fix is neither pages nor one screen but the rule that a page is a few large controls. Knowing which failure you are avoiding is not the same as knowing which one you are walking into.

**The knob the design needed did not exist, and finding that out took reading the widget rather than trusting the render.** The poly face's `ParameterKnob` is the big legible dial; the concept was drawn with it. But it is read-only, and ML-P8 has typed value entry on every control — swapping wholesale would have silently removed a feature nobody asked me to remove. `KnobField` has the typed field but lays label, dial and field in a *row*, 130px wide at a legible size, so four do not fit in a quarter of a face. `KnobStack` is both, and its dial is a real `ParameterKnob` with its captions off so there is one implementation of what a drag or an armed route does. That is now the knob a device page reaches for, and DS-01 inherits it.

**Three rounds of Adam's notes, and the useful ones were about structure rather than taste.** "Why is everything misaligned" was a `TitledModule` whose `@children` landed inside its title row, collapsing two pages onto one line. "Too much space for too little stuff" was two peers drawn at 640px and 200px because Slint gives stretch to whichever has the larger preferred width, so they had to be pinned to explicit halves. "Do we need those bright borders" was a cell drawing an accent border *and* a fill, so a patch with four routes read as a warning light. None of those were visible to me until they were rendered at the real size and someone looked.

**The one thing I invented, I made earn its place.** Adam drew "master vol, pan" in a box on the mockup. Those were new parameters, and the existing code carried a comment saying the device deliberately had no such knobs because the channel strip owns level and position. But `VcaLevel` and `Pan` were already per-voice modulation destinations resolving from *hardcoded* unity and centre — so a Velocity route on Pan swung around dead centre no matter what the patch wanted. Making them real parameters gives those routes an authored base, and Spread now widens around wherever the device sits. The comment that said not to add them was right about the reason and wrong about the conclusion.

## Sep 4 (last) — DS-01's face stops being one screen, and the kit ships

The same ruling, one device later, and DS-01 had walked further into it than ML-P8 had. Its face fitted ninety-two parameters on one screen by making the scopes the **envelope editor** — attack, hold and decay were dots dragged on a curve, not knobs — and everything left over sat at a 21px dial with an 8px caption across five rack units. `08-the-face.md` argued that as the design's central property: "DS-01's controls do not fit on one face unless the scopes are the envelope editor." It is six pages now, at 34px dials, on four units.

**The plan told me it did not fit and I read it as a design.** That sentence is a warning wearing the clothes of a decision. Read forward it says "so here is how it fits"; read plainly it says "this device wants a page." The mockups README even records that DS-01's own concept B was rejected for being "pages of knob rows", the ML-P8 failure — and having named that trap, the plan then walked into the mirror image of it, exactly as ML-P8's one-screen face had. Twice now, on two devices, the same shape: the layout that avoids the failure you have named is not automatically a layout that works.

**Deleting the knobs to make the picture fit cost the device typed entry, and nobody noticed because nothing was removed.** At a 21px dial there is no room for a value field, so DS-01 shipped with ninety-two controls that could be approached and none that could be typed — while ML-M1, ML-P8 and every effect had a field on every knob. It never appeared as a regression because it was never present to regress. The paged face uses `KnobStack`, and one `ds01-text-committed` handler covers all ninety-two, because the descriptor id travels with the text.

**A model-bound control stops following the model the first time you touch it, and no snapshot can see that.** DS-01's face is the one indexed by descriptor id — arrays in, `(id, normalized)` out — so a knob's value is a *binding* onto a model row rather than a property Rust sets. Slint drops a binding at the first assignment to the property it feeds, and assigning to its own value is exactly what a knob does mid-drag. Nothing shows it at the time, because the edit handler writes the same value straight back. It would have shown up the first time somebody loaded a factory preset over a face they had been turning — which is an ordinary action now that there *is* a factory bank, and was not before.

**And the obvious fix for it was wrong, which the test caught and no amount of reading would have.** A `changed` handler re-asserting the model value onto the knob looks airtight and cannot work: a Slint binding is lazy, so once nothing depends on the private property holding the model value, it is never re-evaluated and `changed` never fires. What works is the opposite move — stop the control writing its own value at all. `ParameterKnob` and `MiniKnob` take a `controlled` flag now: report the change, leave the property alone, let the owner write the model. Reading `Property::set` in `i-slint-core` is what settled it, and it also says why no other face has ever had this: a two-way `<=>` binding *intercepts* the write and forwards it up rather than being removed, so every face that declares a property per parameter is exempt by construction. The value then has exactly one home, which is what the array indirection was always implying and what the two-way binding had been quietly denying. I wrote the test expecting it to pass; it failing is the only reason the shipped fix is the right one.

**The panel nobody could design turned out to need room rather than cleverness.** The matrix's thirty-two controls were the one thing `00-status.md` recorded as wanting Adam before it wanted code: three concepts had drawn the columns and the bands and none had drawn this. Given a page of its own it is eight rows of source, destination, amount and curve at full width, and the only real decision was that nine sources and forty-seven destinations want a list rather than a chip you click through — which `PickerChip` already did for ML-P8, one file away. The reason it looked undecidable is that it was being fitted into a band 56px tall.

**The kit's patches already existed, in a test.** Step 09 asks for a factory bank; `mooloop_dsp::ds01` already held thirteen patches reasoned out of the architecture, asserted to sound, terminate, and differ. Promoting them into `ds01_factory` and pointing the test back at it means what ships is what is asserted — seventeen patches now, with the toms as three tunings of one and the ghost snare as the tight snare plus two matrix rows. The Ride is the patch that made the acceptance test better: it is gated, so it rings until it is released, and the loop's "every patch ends" assertion had to learn to send a note-off rather than excuse the one patch that uses the gate.

**And nobody has heard any of it.** That is the honest state of step 09 and it is the half that matters: the bank is checked, not judged. The last recorded listening pass was the ML-M1 bank on 2026-08-31.

## Sep 5 — the line gets redrawn around what the instruments left behind

No code, one comment. Adam brought eight things that were not written down anywhere — the keyboard still needing a click on the background before spacebar works, a left channel sidebar and a redesigned modulation rack drawn in a 1.0 mockup, keyboard navigation and presets in the browser panel, an icon pass, colour schemes, and the question of why simply giving the old drum synth automation support had never been on the table.

That last one turned out to be a fact-checking job rather than a feature request. `generator.rs` said `DrumSynthParams` was a mode-union whose flat table would hand out ids that changed meaning with the Mode switch. It is a flat struct of twenty-one named fields, and `drumsynth.rs` says in its own module comment that the other modes' knobs are retained across a mode change — which is only possible because each field means one thing always. Mode picks which fields are audible, and a voice latches it at note-on. What survives is that a route onto `kick_start_hz` does nothing in Snare mode, which is the situation a route onto a bypassed effect is already in.

So `FOCUS.md` was rewritten, and the shape it took was not the one the new list suggested. Seven of the eight items are interface, but the sequence starts with the debt the instrument push left: ML-P8 step 06 and DS-01 step 07 are the same work — typed auxiliary audio edges — and two finished plan directories are sitting in `plans/` unable to archive because of it. Then the drum synth's table. Then the shell. Buffer stays last, where it has been, because its question is a judgement and judgement wants finished things to judge.

The other correction was quieter: `CURRENT.md`, whose one job is describing what the application does, had never mentioned the browser sidebar, the status bar, or the resizable dock. They shipped on Aug 28 and were written up here, in the journal, and nowhere that anyone reads to find out what exists.

## Patterns worth noticing

**Hardcoded constants drift; derived ones don't.** The 758px viewport, the 220px pattern strip with 190px of hole, the fixed 5px note edge zone that ate a minimum-width note, the forwarded-command threshold of 29 that had overcounted the baseline, the piano roll's C2–C6 range hardcoded as a bare `49` in half a dozen places. Every one was correct on the day it was written; a stale range check in the save validator (checking volume against `0.0..=1.0` after the trim ceiling moved to +12dB) is the same failure one layer over, in validation instead of layout.

**Slint input routing is where the bugs live.** Focus scopes eat the first press. `TouchArea.pressed` only tracks the left button, so right-drag-to-clear cleared exactly one step. Per-element hit areas can't survive a drag that moves the element. Three different symptoms, one lesson: test by dispatching real pointer events, because invoking callbacks directly passes even when routing is completely broken.

**Two silence bugs, no shared cause.** Missing JACK links; swallowed decode errors. The diagnostics harness built to chase the second one is still earning its keep — autodrive now covers the whole effect command surface.

**Write the primitive once when the second user is already visible.** `delayline` for the delay and the buffer device. `dynamics` for three effects. `EffectChain` and `OutputStage` for channels and buses. `KnobFace` for two knob sizes. Descriptor tables instead of a callback per knob. `OnePoleLp`, `AllPass`, `Biquad`, and `scale::hz_from_normalized` for the four filter/frequency-mapping duplicates found once someone actually went looking (`6f5a4e4` → `11241e1`).

**Constraints get relaxed as soon as they're cheap.** The lower-numbered bus rule lasted about a day, and removing it took a topological sort over fixed-size arrays. Worth asking of the remaining rules which ones are still load-bearing.

**A ceiling costs nothing; dimensioning by it costs everything.** Nothing about `MAX_CHANNELS` or `MAX_EFFECTS_PER_CHANNEL` being 256 was wrong. Reserving their *product* before a project existed was, and it cost 42.8 MiB that no individual definition made visible — the number only appeared when they were multiplied. The fix left both ceilings exactly where they were.

**Names are the thing that has to be reachable, not values.** `Osc.phase` was private with no reset and no wrap event, which is why `COMPOSABLE_DEVICE_UNITS.md` used it as its live counter-example. Hard sync for the ML-P8 needed all three, and adding them was mechanical *because the value already existed as a field*. The habit is cheap precisely when it looks unnecessary.

**A position is not an identity.** Routes and lanes named a device by its slot and a channel by its index, and every structural edit silently repointed them. Stating each edit once as a permutation and running it over everything that stores a position is the fix; so is minting durable ids for modules so a grid reorder means nothing at all.

**Deferrals are recorded with reasons.** Limiter lookahead waits on plugin-delay compensation. `ParamAddr`, inter-channel data and audio sidechain are named as deliberate absences. The README states what the mixer deliberately is *not* yet.

**A comment that states a fact can outlive the reason for it.** The sampler's inline SVF and ADSR carried comments justifying why they weren't the shared primitives; asked to actually attempt the merge and measure rather than trust the comments, both conclusions held but for different, sharper reasons than what was written (`5d82121`). Worth periodically re-deriving *why*, not just re-reading it.

**Unattributable CPU load has more than one hiding place.** The gradual jankiness Adam kept noticing turned out to be denormal floats in recursive DSP state going unflushed (`0921c85`) — a different failure shape than the JACK buffer size silently inheriting the server's quantum (`f33bd28`) or `EffectChain` scanning empty slots (`1fd6a3c`), but all three read identically from the outside: no hot function, just slower than it should be.

**A position is not an identity.** Effect slot numbers, channel indices, and modulator slot numbers were all used as durable references by something that outlived the thing they pointed at. Every one of them re-aimed silently on a structural edit rather than failing, which is the worst way for a reference to be wrong — the filter went dry and the LFO drove whatever slid into its seat, with nothing to see. The fix was not better bookkeeping at each call site but stating each edit once as a permutation and running it over everything that stores a position (`0c60828`, `c1765e9`). The modulator grid's version of the bug had no symptom at all, because nothing authors such a route yet; it was worth closing anyway.

**Measure the whole thing, not the part you came to measure.** The modulator-capacity plan measured the modulator arrays, put the render graph at 3.1 MiB, and named control outputs as the biggest line. Measuring the graph found 42.8 MiB, of which `ChannelStrip` was 37.7 and modulation was 433 KiB — so the plan's entire premise was about the smallest column in the table. The correction is a test that pins the arithmetic rather than a paragraph that states it.

**A plan can be overtaken by a better answer, and should say so.** `amortize-reverb-partition-cost` asked for convolution partitions to be spread across blocks. Replacing the convolution with a feedback delay network made the cost flat, 31x smaller at worst case, independent of decay length, *and* made every reverb parameter modulatable — which the old node could not be at all, so every route aimed at a reverb knob had been a silent no-op while the metadata declared six of them legal. The plan was right about the symptom and wrong about the remedy.

**The artifact is the point, when the artifact is what you came for.** Bitcrush is deliberately not oversampled. The stretcher's ratio ceiling is 16 rather than the clean band's 1.5, and the spike's "falls apart at 2.0" verdict is a feature description. Grain mode is defined by removing the correlation search that makes joins phase-coherent. In each case the honest engineering instinct — reduce the error — would have produced a worse instrument, and the clean band became something the UI marks rather than something the DSP refuses.

**Half a contract is worse than none.** The drum synth was left with an empty descriptor table rather than a partial one, on the grounds that the picker would list some of its knobs and silently omit the rest. Ten days later that call is what made the DS-01 argument legible: the table is missing on purpose rather than because nobody got round to it. *Corrected 2026-09-05:* the reason recorded in `generator.rs` for it staying missing — that `DrumSynthParams` is a mode-union — was not true, and had gone eleven days unchecked. It is a flat struct whose fields keep one meaning regardless of the Mode switch, which is precisely why the other modes' knobs survive a mode change. The lesson survives; the fact under it did not, which is its own lesson about how a decision hardens into a premise nobody re-reads.

## Open threads

Refreshed 2026-09-02, with the September documentation audit's threads merged in on 2026-09-04 and Adam's 2026-09-05 list merged in after that. Four of the six threads listed here in August are closed: modulation drives things now, the buffer device exists, undo and clipboard are real, and the convolution reverb that needed an IR loader was replaced outright by an FDN hall — so `StereoIr` is no longer the boundary anything is waiting on.

- The v1 drum synth is still the only generator that cannot be modulated, and it is now scheduled to get a table after all (`FOCUS.md` step 2) — not instead of DS-01, which is finished and is the better instrument, but because the recorded reason it could not have one does not hold. DS-01 itself is done: nine steps, a six-page face, a seventeen-patch bank, played and signed off on 2026-09-04, leaving v1 and its saved projects alone. Only step 07's outlets are open.
- ML-P8 stops inside step 06, and that is now first in the sequence. Its control outlets are declared, published and routable; what is missing is the source picker offering them, so such a route can be written by a project file but not built by hand. The audio half is blocked on typed auxiliary audio edges rather than merely unbuilt — the same gap DS-01's step 07 waits on, which is why doing it once closes two plan directories. Step 07 is a listening pass that needs Adam's ear; ML-P8 has not been played since its pool and paged face landed.
- Buffer Stage 1's acceptance test 8 — no allocations or locks in the callback — is still unverified. It needs an allocation-tracking harness, not a reading of the code.
- The sampler's four-voice stretching polyphony cap is not enforced anywhere: `StretchPool::new` builds a reader for all sixteen voices.
- Acid's Cutoff knob means a different frequency from the other two ML-M1 models — 0.41x nominal against 0.65–0.68x. The compensation constant is load-bearing, not a typo; correcting it lines the corners up and breaks the filter. Lining them up means re-deriving it, and whether it *should* track the others is a taste question Adam has not been asked.
- Mixer: inserts only, no sends, sidechain, solo, stem export, or bus renaming.
- No plugin-delay compensation, so no lookahead anywhere.
- The effect-level preset exists as of 2026-09-04, with a factory bank per kind and save/load on the rack row; what is still missing is the browser, the taxonomy surface, and a factory-content mechanism that can update what it shipped. DS-01's bank is the second instrument those were waiting for, and it landed the same day — as *generator* presets, since a DS-01 patch has no channel rack, which is the contrast with the ML-M1 bank that had to ship as channel presets. The browser now has a destination: Adam wants presets browsable in the sample browser panel (`FOCUS.md` step 3).
- `PolylinePlot` does not exist, and the workaround for it is hand-rolled 17 times across 8 files.
- MIDI input is wired to nothing: there is a JACK port, a decoder, and a `BufferMidiMap` the render state will apply, with no caller installing one and no controls on the MIDI preferences page.
- The Buffer's product question is untested. The device is built and is an ordinary insert; whether routing a source into it and sequencing the result beats bouncing to a sample is what `docs/FOCUS.md` step 4 decides.
- The tooltip audit is unfinished: the status bar exists and about forty sites feed it, but deciding per control which half of the rule it falls under has not happened, and the sampler face is not plumbed in at all.
- The v1 mono synth cannot be deleted until its channels have somewhere to land, which is the poly mono/legato toggle in `docs/plans/poly-v1-mono-mode/`. Until then the picker lists both mono synths.
- Keyboard focus is unreliable and it eats shortcuts, spacebar included. One root `FocusScope` reached by `forward-focus`, so anything focusable inside it consumes the key before the dispatcher runs, and the workaround is clicking a neutral background area first. Suspected since August, confirmed by Adam on 2026-09-05.
- Keyboard navigation exists in the piano roll and nowhere else. The browser tree cannot be reached or driven from the keyboard, and the roll's arrow keys move a selection without being able to build one.
- Channels have no colour — no field in the UI, the session model, or the project format — which is the one genuinely new persisted thing the planned channel sidebar needs.
- The modulation shelf is about to move. `UI_DESIGN.md`'s shelf section describes a location under review, and the 1.0 mockup draws the modulator as a tracker, which is the same notation `IDEAS.md` proposed for automation events. Whether those are one editor or two is unanswered.
