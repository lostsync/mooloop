# Whole-system review, 2026-09-20

Scope: seams (two owners, duplicate abstractions, UI/engine drift), real-time
safety of the audio callback, and architecture fit for automation, recording,
plugin hosting and instrument tracks. Read against `origin/main` at `19c467f`,
one day after `reports/fable-2026-09-19.md`, whose baseline was `e19fb80`.

What landed in between: the whole audio-recording series (steps 02-05 and
01's JACK half, `777e2a0..b9bb4d8`), the `SourceNode` trait (`7cd61f0`,
yesterday's Plan D), the carry plan riding the reclaim ring (`7d4cf34`,
yesterday's Plan A), Add Channel's undo entry (`7562ec3`), and one revert:
`db02366` had applied yesterday's Plan B (the three `ArcSwap` routing tables
become boxes behind structural commands) and `eb4f605` reverted it to merge,
saying so: *"deferred, not abandoned"*.

Method: five Sonnet investigators and one Haiku, each briefed to skip
anything in `docs/LOOSE_ENDS.md` or the three previous reports: (1) the
capture path for RT safety; (2) the rest of the engine diff since `e19fb80`
for RT safety and the `SourceNode` refactor; (3) two-owner take state and
lifecycle holes; (4) `dupe-audit`, doc claims and last report's plans; (5)
the live plans' assumptions against the code; (6) undo, dirty and
persistence coverage of the new verbs. Every kept finding was re-read at the
cited lines by the author. Nothing in the source was changed.

## Findings, ranked by impact

### 1. The `ArcSwap` window is now open every block, on every live channel, and its fix was applied and reverted — MEDIUM (carried over, re-ranked)

Yesterday's finding 2 was a note-on-sized window. Two things widened it
since:

- `RenderState::advance_takes` (`crates/mooloop-engine/src/render.rs:6070`)
  takes `self.audio_input_routing.load()` and holds the guard across the
  whole per-channel loop (`:6073-6097`) on every block in which any strip
  has a live take. It is called from `process_block` (`:6037`) under
  `Executor::process_with_input` (`executor.rs:296`) under the JACK callback
  (`jack_driver.rs:68`).
- `monitors_input` (`render.rs:4984-4988`) takes a fresh guard for *each*
  live channel on *each* block, whether or not anything is recording: the
  render loop asks it at `:5718` to decide whether the channel may sleep.
  That landed in `b9bb4d8`, after yesterday's tree.

The writer is unchanged: `SharedCells::set_audio_input_routing`
(`lib.rs:651-654`) does `store(Arc::new(AudioInputRouting { taps }))` and
keeps nothing, so a guard live at the store is the last owner of a
`Vec<Option<AudioTap>>` (`render.rs:2794-2796`) and frees it in the
callback. The session sends that store on every audio-input pick
(`crates/mooloop-session/src/engine.rs:715`).

`db02366` fixed this exactly as yesterday's Plan B described (three boxes,
three structural commands, displaced box out through the reclaim ring, an
allocation test, the rule written into `AUDIO_ARCHITECTURE.md`), passed
rung 4 on the box, and `eb4f605` reverted it to merge past `7cd61f0`,
stating "deferred, not abandoned". A trial `git cherry-pick -n db02366`
onto `19c467f` (in a throwaway worktree, discarded) conflicts in eleven
hunks: seven in `executor.rs` (the install arm and its tests, where
`7d4cf34` did the same finding-1 work differently), two in `lib.rs:375,476`
(the reclaim enum and `PreparedProject`) and two in `render.rs:3015,3176`
(the `RenderState` fields and constructor). Every one is the finding-1 half
colliding with its twin; the finding-2 half applies clean. Plan A is
therefore "take `db02366`'s finding-2 half by hand", not a cherry-pick.

### 2. A take in flight has no owner at three edges: quit, a failed drain, and a channel that changed kind — MEDIUM

The capture path is clean on the audio thread (below) and careful about
every edge the plan named. Three it did not name:

- **Quit.** `on_quit_requested` (`crates/mooloop-ui/src/lib.rs:5675-5687`)
  and `on_close_requested` (`:6238-6247`) ask about `session.dirty` and
  nothing else. A live take is not dirty. The drain is a detached
  `JoinHandle` (`crates/mooloop-session/src/take.rs:82,147`) that
  `TakeRecorder` never joins on drop (no `Drop` impl in the file); the
  WAV's `finalize()` runs only at the end of `drain` (`:385`). Quit during a
  take ends the process with the header unpatched, and the take is gone
  with no dialog.
- **A failed drain keeps its file.** `drain` removes the file only on
  `Drained::Empty` (`take.rs:388-390`); a `Failed` write or finalize
  (`:353-357,385-387`) returns with the partial file in `recordings/`,
  where step 06 ("find and delete takes nothing refers to") is not started
  (`audio-recording/00-status.md`). The pump logs it (`lib.rs:13527`).
- **The channel is no longer a sampler when the take lands.** `apply_take`
  finds the channel by id (`lib.rs:15002-15013`) and calls
  `apply_loaded_sample` (`:14907-14957`), which checks no kind: it sends
  `set_channel_audio` to the engine slot and writes `sample_name`,
  `sample_data`, `waveform` and the rest onto a channel that
  `reset_channel_source` (`crates/mooloop-session/src/session.rs:432,474-493`)
  cleared when the kind changed. The result is a sample the face cannot show
  and the save cannot write (the `Sampler`-only arm at `session.rs:500-517`),
  a "Record Take" undo entry, and `dirty` for nothing. The channel-gone
  case has the right answer one branch up; this case needs the same one.

One line more of the same shape: `record_press`
(`crates/mooloop-session/src/take.rs:240-256`) arms whenever
`audio_input` is not `Off`, and a source the picker reports `is_missing`
(`crates/mooloop-core/src/input.rs:188`, drawn at `lib.rs:5094`) is not
`Off`. REC on a channel whose source was deleted records a silent take with
no message. Plan B.

### 3. The RECORD page's phase is spelled in Rust and in Slint, and nothing reads both — LOW

`TakeStatus` (`crates/mooloop-engine/src/take.rs:45-50`, atomics) is the
truth; `TakeView` (`crates/mooloop-session/src/take.rs:87-92,165-176`) is a
pass-through. The third copy is the window's `sampler_record_state`, which
`publish_take` (`crates/mooloop-ui/src/lib.rs:5112-5146`) encodes as
`Waiting → 1`, recording `→ 2`, else `0`, and which
`sampler-device.slint:1082-1139` decodes with a ternary ladder
(`record-state == 2 ? "STOP" : record-state == 1 ? "WAIT" : "REC"`, `:1086`).
`record-max-bars: 64` (`:293`) is a second spelling of `MAX_RECORD_BARS`
(`crates/mooloop-core/src/input.rs:208`); Rust overwrites it at `lib.rs:5107`,
so it is a dead default today. `slint_face_agreement.rs` reads
`sampler-device.slint` for the sampler's descriptors and not for the
record page; no test in `crates/mooloop-ui/tests` names `record-state` or
`record-max-bars`. The CLIP and LENGTH toggles already needed a write-back
(`lib.rs:9086-9101`, "a click sets but nothing else does") to keep the
window's copy honest, which is the same fault one level down. This is the
codebase's characteristic fault (`AGENTS.md`, "Duplication") on a page that
is three days old. Plan C.

### 4. Three new verbs on the cheap path, none written down — LOW

`on_audio_input_picked` (`crates/mooloop-ui/src/lib.rs:9049-9064`),
`on_sampler_record_clip_changed` (`:9086-9101`) and
`on_sampler_record_bars_changed` (`:9106-9118`) call `mark_dirty` and no
`record_project_history`. That is the class `docs/LOOSE_ENDS.md:583-590`
records for send edits and `:475-486` for "every effect and generator
parameter", and it is the right call for a knob. The audio input is not a
knob: it is the channel-level choice the MIDI IN row has always made the
same way, so the class holds, but neither row is in the list. Two more
lines for the same document: `Session::input_monitor`
(`crates/mooloop-session/src/session.rs:76`) is never pruned when a channel
goes (no reference outside `take.rs`), harmless because ids are never
reused; and a pasted channel keeps the copied `audio_input`
(`channel_clipboard` clones the whole `ProjectChannel`, `session.rs:1745`;
`rescope_after`, `crates/mooloop-core/src/project.rs:1545-1570`, walks
subscriptions, modulation and lanes and not this field), so a copy of a
channel that resamples itself resamples the original. Whether that is right
is Adam's call; it should be a sentence somewhere. Plan D.

### Seams looked for and not found

- **Channel and track identity.** Unchanged since yesterday. `AudioInputSource`
  names a channel or track by id and `resolve` (`crates/mooloop-core/src/input.rs:54-66`)
  turns a dead id into `None`, so a removed source records silence, never
  the wrong channel. The integrity pass has no `audio_input` line
  (`crates/mooloop-project/src/integrity.rs`, zero matches); it needs none,
  because the picker treats a dead id as `is_missing` and the tap as `None`.
  Kit and channel documents drop the field on the way in
  (`forget_audio_input`, `crates/mooloop-project/src/lib.rs:26,1100,1107`).
- **The take's channel.** A take is keyed by `ChannelId` from arm to apply
  (`record_press` → `TakeRecorder::arm` → `FinishedTake.channel` →
  `apply_take`), and rides its strip across an install
  (`StructuralCommand::StartTake` displaces only a take on the same seat,
  `render.rs:4430-4436`). A project open or undo that rebuilds the ids
  abandons the ring, the drain finalizes (`session take.rs:375-377`), and
  `apply_take` reports the channel gone. Two takes on one channel are
  serialized by `record_press` (`Stop` while live); the decode workers
  (`lib.rs:13530-13540`) apply in completion order, which could only invert
  if a stopped take's decode outlasts the next take's pre-roll and length.
- **`embedded`.** Set at `lib.rs:15016-15018`, cleared by every ordinary
  load (`:14942`) and every kind change (`session.rs:485`), round-trips
  through `SampleReference::File { embedded }` (`session.rs:507,1436`), so
  an undo of "Record Take" restores the previous sample with its flag.
- **Reconciliation after install.** The audio-input table and the monitor
  set are rebuilt from the incoming project and the session's identities on
  every install (`InputState`, `crates/mooloop-engine/src/lib.rs:681-700`;
  `install_project_in_ui`, `lib.rs:14690-14701`); a queued routing message
  for the outgoing document is discarded by `discard_document_messages`
  (`crates/mooloop-session/src/engine.rs:169-206`, tested at `:892-930`).
  No `_sent` cache to go stale.
- **Yesterday's plans.** C (the device header reads `SourceKinds.labels`,
  `main.slint:4543`, asserted by `source_kind_menu.rs:154`), E (`mlm1.rs:445`,
  `polysynth.rs:407` read `OSC_SEMITONE_RANGE`) and the 09-18 Plan B (`NewSong`
  and `Loaded` both call `discard_document_messages`, `lib.rs:13002,13329`)
  all landed. A (`7d4cf34`) landed. B was applied and reverted (finding 1).
- **`dupe-audit`.** 21 leads, the same set as yesterday less the tuning
  line; `one-sided-test` and `bar-arithmetic` clean; `unchecked-face` still
  `bus-device.slint` (`LOOSE_ENDS.md:1225`). `WAVEFORM_BINS` twice is the
  name coincidence noted yesterday.
- **Doc claims.** `CURRENT.md:690-716` on the AUDIO row, the RECORD page,
  CLIP/LENGTH 1-64, the bar-line wait, the clip stop, "Audio In" and MON
  each match a line of code. `CURRENT.md:705` ("one 'Record Take' undo
  step") is true; it does not claim the settings are undoable.

### Real-time safety: the capture path is clean, and the one hazard is finding 1

Two investigators walked from the JACK callback (`jack_driver.rs:61-75`)
through `process_with_input` (`executor.rs:147-380`), `load_input`
(`render.rs:4954-4970`: a bounded slice copy, `frames.min(MAX_BLOCK_SIZE)`),
`process_block`, the `SourceNode` dispatch (`render.rs:2488-2527`, unsized
borrows of fields the strip already owns, no `Box`), the monitor add
(`strip.bus.add_from(&self.input, ..)` under `if monitored`), the
compensation delays, `advance_takes` (`render.rs:6057-6098`: `Option::take`
and put back, never dropped), `Take::advance` and `write`
(`take.rs`: atomics and `rtrb::write_chunk_uninit` on a ring the control
thread allocated), `StartTake`/`StopTake` (`render.rs:4430-4436,4477-4485`:
the displaced take leaves as `StructuralReclaim::Take`, dropped in
`EngineHandle::poll` at `lib.rs:1070`; stop is an atomic store), the install
arm (`executor.rs:258-275`: `PreparedProject` destructured, the plan out
with the retired renderer, measured by
`installing_a_project_allocates_and_frees_nothing`), and the input meter
(`meters.rs`: `fetch_max`/`swap`). `input_latency_frames`
(`jack_driver.rs:388-396`) queries JACK on the control thread at arm time.
Nothing new allocates, locks, blocks or logs. The three `ArcSwap` guards
(`render.rs:5054`, `:5163-5186`, `:6070`, and `:4986` per channel per block)
are the whole of the hazard, and it has a verified fix in `db02366`.

## Architecture

Checked against the code, not the plans' claims.

**The third ownership mechanism now has a permanent reader, and its removal
is in the history.** Yesterday's argument stands and is stronger: the
command stream with its reclaim ring and the `SharedCells` atomics are
tested and documented; the three `ArcSwap`s are read every block now
(finding 1). `db02366` is the design, verified at rung 4, and the only
reason it is not on `main` is a merge order. Plan A is the cheapest item in
this report by a wide margin, and every later reader of the routing
tables (a plugin instrument's MIDI, a second hardware input) inherits
whichever mechanism is there when it is written.

**`SourceNode` fixed the call shape, not the slot.** `7cd61f0` gave the
eight generators one `process_source`/`publish_outlets_into`
(`crates/mooloop-dsp/src/node.rs:329-362`) and collapsed five matches to
three (`render.rs:2489,2513,2587`). `ChannelStrip` still holds eight
concrete fields and `active_source: DeviceKind` (`render.rs:2273-2282`), so
a ninth kind edits the struct, `new` (`:2329`), `reset_sources_to_defaults`
(`:2353`), `push_source_base` (`:2405`), `load_source` (`:2427`) and the
three matches: seven sites in one file, plus `DeviceKind`'s five tables in
`core` and the session's two matches. A plugin instrument cannot be a
concrete field, so `plugin-hosting` blocker 4 / step 09 (a boxed slot) is
forced by the first plugin, and it is cheaper before the layer device adds
an arm to each of the seven than after. Not a Sonnet-alone plan; it is the
next engine step after Plan A, and the trait it needs now exists.

**The plugin plan still says a latency change is an install.**
`plugin-hosting/00-status.md:255-256`: "a latency change that today implies
an install implies a tap-bank rebuild too." `StructuralCommand::SetCompensation`
(`crates/mooloop-engine/src/lib.rs:283`; applied at `render.rs:4390-4394`)
swaps a ring in place with no install, and the session resends lengths
after every install. Yesterday's report said so; `19c467f` re-verified the
document's citations the same day and kept the sentence. Step 04 will be
scoped from that sentence. Plan D.

**Recording is aligned where the plan says.** A channel tap reads
`strip.bus` after its compensation delay (`render.rs:5814-5817`), a track
tap after the bus's (`:5993-5996`), both before `advance_takes` (`:6037`)
and the preview. A hardware take starts `start_delay` frames after its bar
line (`crates/mooloop-engine/src/take.rs:112-120`), from JACK's capture
plus playback latency (`jack_driver.rs:388-396`). Core Audio reports no
input and zero latency (`coreaudio_driver.rs:452-457`), so the Mac has no
AUDIO row for hardware; that is the plan's own "left" item and it is
honest.

**Automation and MIDI out need nothing now.** Lanes are per pattern by
descriptor id; `SamplerRecord` and `AudioInputSource` are plain fields with
no descriptor, so they are outside automation and MIDI learn by
construction, which is right for a record-length and an input choice. No
song-level automation exists and no `ParamOwner` variant reserves a place
for it; Adam's decision 1 expects long-form audio to arrive through it, so
that plan will start from zero. No MIDI output path exists; `EventList` is
a fixed array (`crates/mooloop-dsp/src/event.rs:77-92`).

## Plans

Each is sized for a Sonnet-class model working alone in a task worktree.
Run each new test against the unfixed tree first; a test that passes before
the fix is decoration. Rung 2 after each edit, rung 3 before merging.

### Plan A — Reapply `db02366`'s second half by hand (M, half a day)

1. `git show db02366` and read its message; keep only the finding-2 half.
   The finding-1 half is on `main` as `7d4cf34` and conflicts in eleven
   hunks (`executor.rs` ×7, `lib.rs:375,476`, `render.rs:3015,3176`); do
   not cherry-pick.
2. From that diff, port: `StructuralCommand::SetMidiRouting(Box<MidiRouting>)`,
   `SetAudioInputRouting(Box<AudioInputRouting>)`,
   `SetBufferMidi(Option<Box<BufferMidiMap>>)`; the three plain-box fields
   on `RenderState` in place of `render.rs:3000,3007,3010`; the
   `apply_structural` arms that swap and return the displaced box as a
   `StructuralReclaim` variant, as `SetCompensation` does (`render.rs:4390`);
   the `SharedCells` setters (`lib.rs:613-654`) and `set_buffer_midi_map`
   sending the command; `prepare_render_state` setting the boxes from
   `InputState` on install; `apply_midi` (`:5054`), `play_note` (`:5163`),
   `monitors_input` (`:4986`) and `advance_takes` (`:6070`) reading the
   field with no guard.
3. Tests: port `a_routing_change_frees_nothing_on_the_callback` (it must
   fail on the unfixed tree with a free counted while a note is held or a
   take is live); the routing tests at `render.rs:6363-6818` move from
   `attach_midi_routing(Arc::new(ArcSwap::..))` to the command;
   `an_install_carries_its_own_audio_input_routing` (`lib.rs`) still passes;
   `take_tests.rs` still passes.
4. `docs/AUDIO_ARCHITECTURE.md`: take `db02366`'s paragraph as written
   ("nothing the audio thread reads is reference-counted").
5. Rung 2 `mooloop-engine`; rung 4 before merging (the reclaim enum's
   drain is on the handle every crate uses).

### Plan B — A take is owned at every edge (S, three hours)

1. `crates/mooloop-session/src/take.rs`: `TakeRecorder::finish_all(&mut self)`
   that stops every live take's status, then joins each drain (they end on
   `Ended`/abandoned, `:375-377`) and collects, so every open WAV is
   finalized. Call it from both quit paths (`lib.rs:5675`, `:6238`) before
   `quit_event_loop`; count a live take as a reason to ask, with its own
   wording ("A take is still recording"). Test: arm, push frames, call
   `finish_all`, read the file back with `hound` and check the frame
   count; on the unfixed tree the same sequence with a dropped recorder
   leaves a zero-length header.
2. `drain` (`take.rs:353-357,385-387`): remove the file on `Failed` as it
   does on `Empty`, and say so in the failure string. One test per branch.
3. `apply_take` (`lib.rs:15002-15013`): after the id lookup, check
   `channel.kind == DeviceKind::Sampler`; otherwise the same status message
   as the channel-gone branch, file kept. Test at the session level if the
   lookup moves into `Session` (it should: one fn, `take_target(id) ->
   Result<usize, TakeMiss>`, unit-tested for gone and for kind changed).
4. `record_press` (`take.rs:240-256`): take the picker's `is_missing` as
   an argument, or compute it from the session's rows, and return
   `NoInput` for a missing source. Test.
5. `docs/CURRENT.md` recording section: one sentence each for quit and for
   a source that was deleted.

### Plan C — The record page's phase is one table with a test (S, two hours)

1. `crates/mooloop-core/src/input.rs` (beside `MAX_RECORD_BARS`): a
   `RecordFace { Idle = 0, Waiting = 1, Recording = 2 }` with
   `label()` = `"REC"/"WAIT"/"STOP"` and `from(TakePhase)`; `publish_take`
   (`lib.rs:5112-5146`) reads it.
2. `sampler-device.slint:1082-1139`: keep the ladder (Slint has no enum
   import) but drop the `record-max-bars: 64` default to `0` so the
   Rust write at `lib.rs:5107` is the only value.
3. `crates/mooloop-ui/tests/slint_face_agreement.rs` or a new
   `record_page.rs`: read `sampler-device.slint`, collect every
   `record-state == N` and the three labels, and assert they are exactly
   `RecordFace`'s numbers and strings; assert `record-max-bars` has no
   non-zero literal. Must fail if the ladder's `2` and `"STOP"` are swapped.
4. `scripts/slint-sketch crates/mooloop-ui/ui/sampler-device.slint`; rung 2
   `mooloop-core`; the one test on the box.

### Plan D — Four sentences in the right documents (docs only, one hour)

1. `docs/plans/plugin-hosting/00-status.md:255-256`: a runtime latency
   change is a `StructuralCommand::SetCompensation` from the session, not
   an install; blocker 3 orders identity work that has landed. Say what
   step 04 still needs (a latency the session learns after activation).
2. `docs/LOOSE_ENDS.md`, in the undo paragraph (`:475-486`): the AUDIO
   pick, CLIP and LENGTH are on the cheap path with the MIDI IN pick. One
   line for `input_monitor` never pruned. One line for a pasted channel
   keeping the original's audio input, marked as a question for Adam.
3. `docs/AUDIO_ARCHITECTURE.md` "Capture": the sentence `db02366` wrote,
   once Plan A lands; until then, a sentence that the routing tables are
   the one `Arc` the audio thread still loads.

## What this run did not do

- No Cargo command was run. The one command run against the tree was a
  `git cherry-pick -n db02366` in a throwaway worktree, to measure the
  conflict before recommending a cherry-pick; it was discarded unmerged.
- Finding 2's quit claim rests on `TakeRecorder` having no `Drop` and no
  join site, not on a run; `hound`'s own `Drop` finalizes a writer, but a
  process exit runs no destructors on the drain thread.
- The Core Audio input stream, step 06 and the boxed source slot are
  planned work with owners, and are not re-planned here.
- The local clone's `main` was again an unrelated history and was renamed
  `main-stale-local`; this report was written on `origin/main` at
  `19c467f` and pushed fast-forward.
