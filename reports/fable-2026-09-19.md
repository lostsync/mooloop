# Whole-system review, 2026-09-19

Scope: seams (two owners, duplicate abstractions, UI/engine drift), real-time
safety of the audio callback, and architecture fit for automation, recording,
plugin hosting and instrument tracks. Read against `main` at `e19fb80`, one
day after `reports/fable-2026-09-18.md`.

Method: five parallel Sonnet investigators and one Haiku, each briefed to skip
anything in `docs/LOOSE_ENDS.md` or the two previous reports: (1) the
incremental-install carry-over path for RT safety; (2) a systematic RT sweep
from the driver callbacks down; (3) two-owner state, channel identity, undo
and dirty coverage of the new verbs; (4) `dupe-audit`; (5) the live plans'
assumptions against the code; (6) the UI/engine seam. Every kept finding was
re-read at the cited lines by the author. Nothing in the source was changed.

## Findings, ranked by impact

### 1. Every structural edit frees four `Vec`s on the audio thread, and the test that says otherwise never measures the path — MEDIUM

`PreparedProject` (`crates/mooloop-engine/src/lib.rs:395-415`) carries the
incoming renderer as a `Box` and the carry plan as four `Vec<(u8, u8)>`
(`CarryPlan`, `lib.rs:426-439`), built by `collect()` on the control thread
(`lib.rs:709-718`) for **every** install that has a live project
(`install_project`, `lib.rs:1120-1122,1150`), not only for the ones that keep
the transport. On the audio thread, `Executor::process`
(`crates/mooloop-engine/src/executor.rs:220-255`) moves `prepared.render` out
with `mem::replace` (`:244`) and pushes it down the reclaim ring, then reads
`prepared.carry` (`:243`) and `prepared.generation` (`:253`), and the loop
body ends. `prepared` still owns `carry`. Four `free()` calls run in the
callback, one per non-empty vector, on every channel or track add, remove,
move, paste and undo. (A document open whose ids do not survive builds four
empty vectors, which free nothing; that path is not the cheap one anyway.)

The contract this breaks is the project's own: `CarryPlan`'s doc says
"allocated here off-thread" (`lib.rs:434`), `carry_strips_from` says "above
all no allocation" (`executor.rs:623-627`), and `StructuralReclaim::RenderState`
exists precisely so that "the box is destroyed when `EngineHandle::poll`
drains this variant" (`lib.rs:353-356`). The allocation test written for the
feature, `carrying_strips_allocates_nothing` (`executor.rs:631-664`), wraps
`COUNTING` around the bare `carry_strips_from` call (`:648-650`), not around
`Executor::process` with an `InstallProject` in the queue, so the frees
happen outside the window it measures. It is the one-sided guard AGENTS.md
describes: green, correct about what it reads, and not reading the copy that
fails.

Cost today is four small frees per edit, which on glibc is an arena lock
each. It is not a crash. It is the class the last two weeks removed from the
sampler, the reclaim vector and MLP8's chorus, reintroduced by the feature
built to make edits cheap, in the block that feature runs in. Plan A.

### 2. Two `ArcSwap` guards on the audio thread can be the last owner of the value the control thread just replaced — LOW today, higher the day recording reads a third

`RenderState::apply_midi` holds `self.buffer_midi.load()` for the whole
message loop (`crates/mooloop-engine/src/render.rs:4969-4971`; the shadowing
`let map = map.as_deref().copied()` does not end the guard's scope), and
`play_note` holds `self.midi_routing.load()` across its channel scan
(`render.rs:5078-5100`). The writers are `SharedCells::set_midi_routing`
(`crates/mooloop-engine/src/lib.rs:613-616`) and `set_buffer_midi_map`
(`lib.rs:1273-1275`): each does `store(Arc::new(..))` and keeps no other
reference to the value it replaces.

arc-swap's hybrid strategy settles a live guard at `store` by handing it a
full `Arc`; the writer then drops its own. When the store lands while the
callback is inside `play_note`, the guard's drop is the last reference and
`MidiRouting { routes: Vec<..> }` (`render.rs:3002`) is freed on the audio
thread, two frees. The window is one note-on's channel scan against a picker
change or a port hot-plug, so the odds are low and the object is small. The
comment at `lib.rs:1272`, "built and dropped on this thread; the audio thread
only loads it", is the belief that is not quite true.

This is the same mechanism `reports/fable-2026-09-17.md` finding 2 found on
`ChannelAudioSlot` (also an `ArcSwapOption`, `render.rs:105`), fixed by
`load_full` plus retirement through the reclaim ring. Two of the three
`ArcSwap`s in `RenderState` (`render.rs:2995-3009`) still use the guard.
The third, `audio_input_routing`, is read by nothing yet
(`render.rs:4906-4912`) and `audio-recording/03` will read it every block,
which turns a note-on-sized window into a permanent one. Plan B.

### 3. Yesterday's two UI findings are still open, and the documentation has caught up with one of them — MEDIUM (carried over)

`on_add_channel_clicked` (`crates/mooloop-ui/src/lib.rs:8052-8094`) still has
no `record_project_history` call; `queue_channel_insert` (`lib.rs:1446-1458`)
still does. `DocumentResult::NewSong` (`lib.rs:12699-12728`) still installs
without the `survives_project_load` filter that `Loaded` applies
(`lib.rs:13025`, the file's only call). `reports/fable-2026-09-18.md` findings
1 and 2, Plans B and C, neither applied. The 24 commits since then landed
Plans A, D-as-docs and E and the Sonnet-sized ones were passed over.

One thing moved: `docs/CURRENT.md:1616-1617` now says "except the toolbar's
Add Channel, which records nothing". That is the right sentence for today
and the wrong permanent state; a documented gap in the undo stack stays a
gap the user hits. The plans are re-listed below unchanged, C first.

### 4. The source device's header title is a third copy of the kind-label list, and the only one no test reads — MEDIUM

Commit `3fff067` unified the toolbar picker and the rack's `+` menu on
`SourceKinds.labels` (`crates/mooloop-ui/ui/channel-rack.slint:61-65`) and
wrote `tests/source_kind_menu.rs`, which reads both `.slint` files and holds
the list against `DeviceKind::label()` and `device_kind_to_int`
(`crates/mooloop-ui/src/lib.rs:2216-2247`). The same commit left the
`DeviceHeader` in the device rack spelling its own eight strings as a ternary
ladder on `root.source-kind` (`crates/mooloop-ui/ui/main.slint:4485-4491`).
`source_kind_menu.rs` never looks at it; nothing does. Adding a ninth source
kind (the layer device `plans/README.md` says is wanted, or a plugin
instrument) extends two guarded lists and a compile-time match, and the
header keeps saying "Aux In" for every kind past the seventh.

Fix is one line: `name: SourceKinds.labels[root.source-kind]`, and the
existing test covers it once it asserts the header reads the global. The
comment on `add-channel-clicked` (`main.slint:1036`, "0=Sampler, 1=Drum,
2=Mono, 3=Poly") lists four of the eight values the callback has sent since
the same commit; fix it in passing. Plan C.

### 5. The DSP clamps still spell the ranges the descriptors now own — LOW

`eadd445` (yesterday's Plan A) gave the oscillator range one home,
`OSC_SEMITONE_RANGE`/`OSC_CENT_RANGE` (`crates/mooloop-core/src/generator.rs:402-406`),
and a face test that reads it. The per-voice tuning code still clamps to
literals: `crates/mooloop-dsp/src/mlm1.rs:442` and `polysynth.rs:404`
(`.clamp(-48.0, 48.0)`, `.clamp(-100.0, 100.0)`), and the drum tune
descriptor's `-48..48` (`generator.rs:207-208`) is repeated at
`drumsynth.rs:251`, `sampler.rs:85` and `crates/mooloop-ui/src/lib.rs:11179`.
`dupe-audit repeated-line` reports the `mlm1`/`polysynth` line
(three files). Widen a descriptor and the knob travels where the voice
will not follow; nothing reads the pair. Two hours, and it belongs in the
same worktree as the unchecked-face widening Plan A of yesterday asked for.

### Seams looked for and not found

- **Channel and track identity.** `ChainKey::Channel(ChannelId)`
  (`crates/mooloop-core/src/mixer.rs:69`), the envelope gate's `input_channel_id`
  with `identify_gates`/`reseat_gates` (`crates/mooloop-core/src/modulation.rs:2094-2135`)
  wired through `Project::rescope_after` (`project.rs:1541-1564`), Aux In
  (`aux_in.rs:79-117`) and control bindings (`control.rs:203-215`) all name a
  channel by id. The one positional field left, `channel.setup.channel.bus`,
  is the documented exception in `incremental-structure/00-status.md` and is
  rewritten by `rescope_tracks_after` (`project.rs:1483-1500`) on every track
  edit. The claim in `plans/README.md` holds.
- **Session mirrors.** `replace_project` rebuilds every `ChannelState`
  including the new `audio_input` (`crates/mooloop-session/src/session.rs:1403-1409`);
  the five `_sent` reconciler caches are unchanged since yesterday's check.
  The audio-input routing has no `_sent` cache to go stale
  (`engine.rs:244`, a one-shot message).
- **New verbs.** `Session::set_channel_audio_input`
  (`crates/mooloop-session/src/midi.rs:122-131`) is the only session verb
  added since `f2f2dfd`, and nothing in `lib.rs` or any `.slint` calls it
  yet: `audio-recording/00-status.md` says the drawing is step 05. No new
  `EngineCommand`/`StructuralCommand` variants since `f2f2dfd`, so the
  `install_project` audit of yesterday stands.
- **Hand-set `dirty`.** Ten sites now (`lib.rs:8611,8634,8696,8723,8745,
  11979,12072,13062,13319,14516`), up from four; every one is followed by a
  command or an inline `revision` bump, so the save-race still closes. Same
  nit as yesterday, not a finding.
- **`dupe-audit`.** 22 leads. `one-sided-test` and `bar-arithmetic` clean.
  `unchecked-face` reports `bus-device.slint` only (already
  `LOOSE_ENDS.md:1225`). `shared-constant`'s `WAVEFORM_BINS` twice
  (`session.rs:350`, `buffer_device.rs:50`) is a name coincidence: the
  sampler overview and the Buffer's peak array are read independently and
  need not agree. `repeated-line`'s `mlm1`/`polysynth` tuning line is
  finding 5; the rest is copied arithmetic and test assertions.
- **`main.slint:6419`'s `mod(bar, 4)`** shades every fourth playlist bar. It is
  a phrase grouping, not beats-per-bar; the `bar-arithmetic` check is right
  not to see it.
- **`docs/CURRENT.md` and `docs/ACTIONS.md`** claims for the source picker
  (`CURRENT.md:123-124`), Add Channel (`:1616`) and four sampled actions in
  `actions.rs` all match the code.

### Real-time safety: swept, two findings, both at ownership edges

Two investigators walked the callback again: JACK's `ProcessHandler::process`
(`jack_driver.rs:56-68`), the Core Audio `render_callback`
(`coreaudio_driver.rs:660-711`, the `try_lock` at `:677` unchanged), every
`RealtimeCommand` arm of `Executor::process`, `apply_command`'s
`get_mut`-only indexing (`render.rs:4439-4900`), `apply_structural`'s
`SetCompensation`/`SetTrackGraph` (`render.rs:4380-4418`, which hand a ring
back rather than drop it), `SendBank::adopt_rings_from` (`render.rs:433-479`),
`Sequencer`'s bounds checks, the whole Buffer diff (`adbd9fe`: a fixed
`[Option<f64>; 3]` replaced an enum, nothing heap), the `ParamKey` work in
`modulation.rs` (control thread only, `project.rs:1123,1156`), and
`Sampler::swap_audio_slot_with` (`sampler.rs:487-506`, a pointer swap).
`held_keys` and `recording` cross the swap as `Copy` arrays
(`render.rs:2701,3022`). The retired `RenderState` is dropped in
`EngineHandle::poll` (`lib.rs:1012-1032`).

Everything they found clean *is* clean. What they missed sat one line
outside the function each doc comment describes: finding 1 is the struct
around `carry_strips_from`, finding 2 is the guard around `load()`. The
`carry_plan` doc says "no allocation" of the right function and the wrong
scope. A reading of a function is not a reading of its caller's drop order.

One cost note, not a hazard: `Sampler::shape_frame` takes two `powf` per
voice per sample (`sampler.rs:972,999`), the second unconditionally.

## Architecture

Checked against the code, not the plans' claims.

**Three ownership mechanisms cross the control/audio boundary, and both
findings sit on the third.** The command stream with the reclaim ring
(effects, whole renderers, samples, compensation rings: every displaced
object goes back up, `lib.rs:351-363`); the `SharedCells` atomics (meters,
keyboard channel, xruns); and three `ArcSwap`s in `RenderState`
(`render.rs:2995-3009`) for the tables the MIDI path reads. The first has a
test for every variant and a doc rule. The third has a comment that is
wrong (`lib.rs:1272`) and one previous bug (2026-09-17). `audio_input_routing`
was added on the third mechanism this week and `audio-recording/03` will
read it every block. The cheap move is to have no third mechanism: the
routing tables are already carried by `InputState`
(`lib.rs:649-664`) into the prepared renderer, so a `StructuralCommand`
carrying a `Box` and returning the displaced one through the ring, the way
`SetCompensation` already does (`render.rs:4380-4394`), replaces all three
cells without a new pattern. Do it before step 03 reads the tap table. Plan B.

**A runtime latency change already has a mechanism, and the plugin plan does
not use it.** `EffectKind::latency_frames` (`crates/mooloop-core/src/effect.rs:62`)
is static and `install_compensation` (`render.rs:3739`) runs per install, as
`plugin-hosting/00-status.md` blocker 3 says. But since `77c75b2` a
compensation ring is decided by its length and swapped in place by
`StructuralCommand::SetCompensation` without an install, and the session's
reconciler already resends lengths after every install
(`render.rs:4390-4394`). A plugin reporting a new latency after activation
is a `SetCompensation` from the session, not a project rebuild. Blocker 3
should say so; it currently orders identity work first that has now landed.

**Source dispatch is still five closed matches.** `match self.active_source`
at `render.rs:2478,2491,2559,2586,2621`, eight arms each, three method
shapes (`process`/`process_publishing`/`process_from`), plus three in the
session (`channel.rs:122`, `session.rs:457,493`). Yesterday's Plan D did
not land. It remains the step before `plugin-hosting/09` and before the
layer device, and its cost grows by one arm per source added in between.

**Recording's input is still absent at the driver.** `Executor::process`
takes MIDI and two outputs (`executor.rs:131-136`); JACK registers
`out_l`, `out_r`, `midi_in` (`jack_driver.rs:205-213`); Core Audio builds an
output stream only. `audio-recording/01` knows. The AUDIO row's command
plumbing landed ahead of it and is inert, which is the right order; the
only thing to change first is which mechanism carries it (above).

**Automation, MIDI output and the project format need nothing now.**
Automation lanes exist per pattern, resolve per control tick by descriptor
id (`render.rs:1349`), and the write-precedence table is a rule
(`render.rs:1296-1306`; `fable-2026-09-17` Plan 4 landed). No MIDI output
path exists anywhere in the engine; a CLAP instrument needs the per-channel
`EventList` it already has, and MIDI out is correctly `SCOPE.md`'s 0.2.0.
`FORMAT_VERSION` is `1` (`crates/mooloop-project/src/lib.rs:38`) and every
persisted addition so far has been a `#[serde(default)]` field; a plugin
state map fits the same way.

## Plans

Each is sized for a Sonnet-class model working alone in a task worktree.
Run each new test against the unfixed tree first; a test that passes before
the fix is decoration. Rung 2 after each edit, rung 3 before merging.

### Plan A — The carry plan leaves through the reclaim ring, and a test measures the whole swap (S, two hours)

1. `crates/mooloop-engine/src/lib.rs:351-363`: change
   `StructuralReclaim::RenderState(Box<RenderState>)` to carry the plan too,
   e.g. `RenderState { retired: Box<RenderState>, carry: CarryPlan }`, or add
   a `CarryPlan(CarryPlan)` variant. Update the drain in
   `EngineHandle::poll` (`lib.rs:1012-1032`), which just drops it.
2. `crates/mooloop-engine/src/executor.rs:220-255`: destructure
   `let PreparedProject { render, generation, keep_transport, carry } = prepared;`
   at the top of the install arm so nothing owning heap is left to drop at
   the end of the loop body; push `carry` with the retired renderer. The
   ring-capacity check at `:222` already reserves one slot; if a second
   variant is used, check for two.
3. Test, beside `carrying_strips_allocates_nothing` (`executor.rs:631`): build
   an executor with the `executor()` helper (`:408`), queue an
   `InstallProject` whose `carry` is non-empty (use `carry_plan` on a moved
   channel as the existing test does), take `COUNTING.allocations()` and
   `frees()`, call `executor.process(...)` once, and assert both unchanged.
   `COUNTING` is the crate's `#[global_allocator]` (`lib.rs:118`). It must
   fail on the unfixed tree with `frees` up by the number of non-empty
   vectors; then pass.
4. `lib.rs:434` and `executor.rs:623-627`: the doc comments are true again;
   add one sentence saying the plan rides the reclaim ring.
5. Verify: rung 2 `mooloop-engine`; rung 4 before merging (the reclaim enum
   is `pub(crate)`, but `poll` is on the handle every crate uses).

### Plan B — The routing tables are structural commands, not `ArcSwap` cells (M, one day)

1. `crates/mooloop-engine/src/lib.rs`: add `StructuralCommand::SetMidiRouting(Box<MidiRouting>)`,
   `SetBufferMidi(Option<Box<BufferMidiMap>>)` and
   `SetAudioInputRouting(Box<AudioInputRouting>)`; `RenderState` holds each as
   a plain `Box`/`Option<Box>` field; `apply_structural` (`render.rs:4198`)
   swaps and returns the displaced box as a new `StructuralReclaim` variant,
   exactly as `SetCompensation` does (`render.rs:4380-4394`).
2. `SharedCells::set_midi_routing` / `set_audio_input_routing`
   (`lib.rs:613-622`) and `set_buffer_midi_map` (`lib.rs:1273`) send the
   command instead of storing. Delete the three `Arc<ArcSwap>` fields
   (`render.rs:2995-3009`), `attach_midi_routing`, `attach_audio_input_routing`
   (`render.rs:4900-4912`) and the `buffer_midi` attach at `:4885`.
   `prepare_render_state` already receives `InputState` and can set the
   boxes directly for an install.
3. `apply_midi` (`render.rs:4969`) and `play_note` (`render.rs:5078`) read
   the field; no guard.
4. Tests: the routing tests at `render.rs:6200-6655` construct
   `ArcSwap::from_pointee(MidiRouting {..})` and call `attach_midi_routing`;
   change them to the command. `an_install_carries_its_own_audio_input_routing`
   (`lib.rs:1604-1637`) must still pass. Add one allocation test: queue a
   `SetMidiRouting` while a note is held, `process` one block, `COUNTING`
   unchanged.
5. `docs/AUDIO_ARCHITECTURE.md`: state the rule the code now has: two
   mechanisms cross the boundary, the ordered stream with its reclaim ring
   and the atomics; nothing the audio thread reads is reference-counted.
   Note it in `audio-recording/03-capture.md` as the table step 03 reads.
6. Verify: rung 2 `mooloop-engine`; rung 3 before merging.

### Plan C — The device header reads the one kind-label list (S, one hour)

1. `crates/mooloop-ui/ui/main.slint:4485-4491`: `name: SourceKinds.labels[root.source-kind];`
   (`SourceKinds` is exported from `channel-rack.slint:61`; check the import
   at the top of `main.slint`). `main.slint:1036`: rewrite the comment as
   "a `SourceKinds.labels` index".
2. `crates/mooloop-ui/tests/source_kind_menu.rs`: add an assertion that the
   `DeviceHeader` block under `if !root.editing-bus` reads
   `SourceKinds.labels[`, and that no other `"Aux In"` literal remains in
   `main.slint`. Confirm it fails on the unfixed tree.
3. `scripts/slint-sketch crates/mooloop-ui/ui/main.slint` to type-check
   (about 3 s); `scripts/exit-code cargo test -p mooloop-ui --test
   source_kind_menu` on the box.

### Plan D — Yesterday's Plans B and C, as written (S, two hours each)

`reports/fable-2026-09-18.md` Plan C (Add Channel records an undo entry,
`lib.rs:8052-8094`) and Plan B (one drain-and-filter for Open and New,
`lib.rs:12699` and `:13025`). Both unchanged and both re-verified today.
When C lands, `docs/CURRENT.md:1616-1617` and `LOOSE_ENDS.md:440` change
in the same commit.

### Plan E — The voice reads the descriptor's range (S, two hours)

1. `crates/mooloop-dsp/src/mlm1.rs:442` and `polysynth.rs:404`: clamp to
   `OSC_SEMITONE_RANGE`/`OSC_CENT_RANGE` from `mooloop_core` (already
   exported, `core/src/lib.rs:126-127`).
2. `crates/mooloop-core/src/generator.rs:207-208`: lift the drum tune range
   to a `pub const DRUM_TUNE_RANGE`, read it in the descriptor and at
   `crates/mooloop-dsp/src/drumsynth.rs:251`; find the sampler tune
   descriptor the same way and read it at `sampler.rs:85` and
   `crates/mooloop-ui/src/lib.rs:11179`.
3. No new test: a value read from one place cannot drift. Confirm
   `scripts/dupe-audit repeated-line` no longer lists the tuning line.
4. Verify: rung 2 `mooloop-core`, `mooloop-dsp`; `cargo check -p mooloop-ui`.

## What this run did not do

- No Cargo command was run. Every finding is a reading, checked twice; the
  frees in finding 1 follow from Rust's drop order, not from a measurement,
  which is what Plan A's step 3 supplies.
- Finding 2's arc-swap semantics are from the crate's documented hybrid
  strategy (`arc-swap 1.9.2`, `Cargo.lock:276`); the registry source was
  not available in this container to quote.
- One investigator recommended landing `channel-identity/05` before
  `plugin-hosting/04`; it landed on 2026-09-18 and is archived. Dropped.
- The local clone's `main` was an unrelated history and was renamed to
  `main-stale-local`; this report was written on `origin/main` at
  `e19fb80` and pushed fast-forward.
